# Руководство по использованию команд платформы ИИ

В этом документе подробно описаны методы использования команд с косой чертой на различных платформах ИИ в Novel Writer.

## 📋 Сводная таблица

| Платформа ИИ | Формат команды | Пример | Пространство имен |
|---------|---------|------|----------|
| **Gemini CLI** | `/novel:имя_команды` | `/novel:write` | `novel:` |
| **Claude Code** | `/novel.имя_команды` | `/novel.write` | `novel.` |
| **Cursor** | `/имя_команды` | `/write` | Нет |
| **Windsurf** | `/имя_команды` | `/write` | Нет |
| **Roo Code** | `/имя_команды` | `/write` | Нет |
| **GitHub Copilot** | `/имя_команды` | `/write` | Нет |
| **Qwen Code** | `/имя_команды` | `/write` | Нет |
| **OpenCode** | `/имя_команды` | `/write` | Нет |
| **Codex CLI** | `/novel-имя_команды` | `/novel-write` | `novel-` |
| **Kilo Code** | `/имя_команды` | `/write` | Нет |
| **Auggie CLI** | `/имя_команды` | `/write` | Нет |
| **CodeBuddy** | `/имя_команды` | `/write` | Нет |
| **Amazon Q** | `/имя_команды` | `/write` | Нет |

## 🎯 Зачем использовать пространства имен?

Novel Writer использует префиксы пространств имен (namespace) на некоторых платформах ИИ по следующим причинам:

1. **Предотвращение конфликтов команд**: Чтобы избежать совпадения имен команд с другими инструментами (например, spec-kit, OpenSpec и т. д.).
2. **Четкая принадлежность команды**: Чтобы пользователи точно знали, что это команда, предоставленная Novel Writer.
3. **Ограничения платформы**: Некоторые платформы не поддерживают организацию по подкаталогам и требуют использования префиксов для различения.

### Правила пространств имен

- **Gemini CLI**: Использует пространство имен подкаталога `novel/`, формат команды: `/novel:имя_команды`
  - Причина: Подкаталоги Gemini автоматически преобразуются в пространства имен с двоеточием.
  - Путь: `.gemini/commands/novel/write.toml` → `/novel:write`

- **Claude Code**: Использует префикс имени файла `novel.`, формат команды: `/novel.имя_команды`
  - Причина: Claude не поддерживает пространства имен подкаталогов.
  - Путь: `.claude/commands/novel.write.md` → `/novel.write`

- **Codex CLI**: Использует префикс имени файла `novel-`, формат команды: `/novel-имя_команды`
  - Причина: Подсказки Codex не поддерживают подкаталоги, использование префикса с дефисом облегчает идентификацию.
  - Путь: `.codex/prompts/novel-write.md` → `/novel-write`

- **Другие платформы**: Без пространства имен, используется непосредственно имя команды.
  - Причина: Эти платформы обычно представляют собой изолированные среды проектов с низким риском конфликтов.

---

## 🚀 Справочник команд Gemini CLI

### Формат команды
```bash
/novel:имя_команды [параметры]
```

### Команды методологии семи шагов

#### 1. Создание конституции
```bash
/novel:constitution
```
Определяет основные принципы творчества, устанавливая высшие руководящие принципы для процесса создания.

#### 2. Спецификация истории
```bash
/novel:specify [описание истории]
```
Четко определяет, что должно быть создано, определяя историю как документ с требованиями к продукту.

Пример:
```bash
/novel:specify Фэнтезийный роман о совершенствовании бессмертных
```

#### 3. Уточнение решений
```bash
/novel:clarify
```
Интерактивно уточняет ключевые точки принятия решений, проясняя неясные моменты.

#### 4. Планирование создания
```bash
/novel:plan
```
Разрабатывает технический план, определяя, как реализовать спецификацию истории.

#### 5. Декомпозиция задач
```bash
/novel:tasks
```
Создает список выполнимых задач, систематизируя процесс создания.

#### 6. Написание глав
```bash
/novel:write [описание главы]
```
Выполняет написание главы на основе списка задач, автоматически загружая контекст.

Пример:
```bash
/novel:write Глава 1
/novel:write Главный герой впервые входит в мир совершенствования
```

#### 7. Комплексная проверка
```bash
/novel:analyze
```
Проводит всестороннюю проверку качества, обеспечивая единообразие качества создания.

### Команды управления отслеживанием

#### Инициализация системы отслеживания
```bash
/novel:track-init
```
Инициализирует систему отслеживания персонажей, сюжета, временной шкалы и т. д.

#### Комплексное отслеживание
```bash
/novel:track
```
Комплексное отслеживание прогресса и интеллектуальный анализ для просмотра состояния создания.

#### Проверка сюжета
```bash
/novel:plot-check
```
Проверяет логическую последовательность сюжета, выявляя потенциальные проблемы.

#### Управление временной шкалой
```bash
/novel:timeline
```
Управляет и проверяет временную шкалу истории.

#### Управление отношениями между персонажами
```bash
/novel:relations
```
Отслеживает и обновляет сеть отношений между персонажами.

#### Проверка мировоззрения
```bash
/novel:world-check
```
Проверяет согласованность настроек мировоззрения.

### Команды экспертного режима

#### Активация эксперта
```bash
/novel:expert [тип эксперта]
```
Получает углубленное руководство в конкретной области.

Пример:
```bash
/novel:expert plot      # Эксперт по структуре сюжета
/novel:expert character # Эксперт по развитию персонажа
/novel:expert style     # Эксперт по стилистической доработке
```

### Другие вспомогательные команды

#### Чек-лист
```bash
/novel:checklist
```
Генерирует чек-лист перед написанием.

---

## 🔧 Справочник команд Claude Code

### Формат команды
```bash
/novel.имя_команды [параметры]
```

### Список основных команд

Все команды имеют ту же функциональность, что и в Gemini CLI, просто замените `/novel:` на `/novel.`:

```bash
# Методология семи шагов
/novel.constitution
/novel.specify [описание истории]
/novel.clarify
/novel.plan
/novel.tasks
/novel.write [описание главы]
/novel.analyze

# Управление отслеживанием
/novel.track-init
/novel.track
/novel.plot-check
/novel.timeline
/novel.relations
/novel.world-check

# Экспертный режим
/novel.expert [тип эксперта]

# Другое
/novel.checklist
```

---

## 📟 Справочник команд Codex CLI

### Формат команды

```bash
/novel-имя_команды [параметры]
```

### Список основных команд

Все команды имеют ту же функциональность, что и в Gemini CLI, просто замените `/novel:` на `/novel-`:

```bash
# Методология семи шагов
/novel-constitution
/novel-specify [описание истории]
/novel-clarify
/novel-plan
/novel-tasks
/novel-write [описание главы]
/novel-analyze

# Управление отслеживанием
/novel-track-init
/novel-track
/novel-plot-check
/novel-timeline
/novel-relations
/novel-world-check

# Экспертный режим
/novel-expert [тип эксперта]

# Другое
/novel-checklist
```

### Особые примечания

**Поддержка Codex CLI** (v0.19.0+):
- ✅ Использование чистого формата Markdown (без YAML frontmatter)
- ✅ Файлы команд находятся в каталоге `.codex/prompts/`
- ✅ Использование префикса `novel-` для предотвращения конфликтов с другими инструментами
- ✅ Полная поддержка всех функций команд Novel Writer

**Способ установки**:
```bash
novel init my-novel --ai codex
```

**Важные замечания**:
- Функция пользовательских подсказок Codex CLI требует, чтобы файлы команд были в чистом формате Markdown.
- Используйте дефис `-`, а не точку `.` или двоеточие `:` в качестве разделителя пространства имен.
- Все выполнение скриптов обрабатывается ИИ автоматически, ручная настройка не требуется.

---

## 💡 Справочник команд для других платформ ИИ

### Формат команды
```bash
/имя_команды [параметры]
```

### Список основных команд

Платформы Cursor, Windsurf, Roo Code, GitHub Copilot, Qwen Code, OpenCode, Codex CLI, Kilo Code, Auggie CLI, CodeBuddy, Amazon Q и другие используют одинаковый формат команд без префикса:

```bash
# Методология семи шагов
/constitution
/specify [описание истории]
/clarify
/plan
/tasks
/write [описание главы]
/analyze

# Управление отслеживанием
/track-init
/track
/plot-check
/timeline
/relations
/world-check

# Экспертный режим
/expert [тип эксперта]

# Другое
/checklist
```

---

## 📖 Примеры использования

### Полный рабочий процесс Gemini CLI

```bash
# 1. Установить принципы создания
> /novel:constitution

# 2. Определить спецификацию истории
> /novel:specify Фантастический триллер о путешествиях во времени

# 3. Уточнить ключевые решения
> /novel:clarify

# 4. Разработать план создания
> /novel:plan

# 5. Сгенерировать список задач
> /novel:tasks

# 6. Начать написание
> /novel:write Глава 1: Возвращение во времени

# 7. Регулярно проверять качество
> /novel:analyze

# Управление отслеживанием
> /novel:track
> /novel:timeline
```

### Полный рабочий процесс Claude Code

```bash
# Используйте тот же процесс, просто замените префикс команды
> /novel.constitution
> /novel.specify Фантастический триллер о путешествиях во времени
> /novel.clarify
> /novel.plan
> /novel.tasks
> /novel.write Глава 1: Возвращение во времени
> /novel.analyze
```

### Полный рабочий процесс Codex CLI

```bash
# Используйте префикс с дефисом
> /novel-constitution
> /novel-specify Фантастический триллер о путешествиях во времени
> /novel-clarify
> /novel-plan
> /novel-tasks
> /novel-write Глава 1: Возвращение во времени
> /novel-analyze
```

### Полный рабочий процесс Cursor

```bash
# Без префикса, используйте непосредственно имя команды
> /constitution
> /specify Фантастический триллер о путешествиях во времени
> /clarify
> /plan
> /tasks
> /write Глава 1: Возвращение во времени
> /analyze
```

---

## 🔍 Часто задаваемые вопросы

### В: Почему моя команда не работает?

**О**: Пожалуйста, проверьте следующие пункты:

1. **Подтвердите формат команды**:
   - Gemini CLI использует `/novel:имя_команды`
   - Claude Code использует `/novel.имя_команды`
   - Другие платформы используют `/имя_команды`

2. **Проверьте наличие файла команды**:
   ```bash
   # Gemini
   ls .gemini/commands/novel/

   # Claude
   ls .claude/commands/ | grep novel.

   # Cursor
   ls .cursor/commands/
   ```

3. **Убедитесь, что проект инициализирован**:
   ```bash
   ls .specify/config.json
   ```

### В: Можно ли настроить префикс команды?

**О**: В настоящее время пространства имен фиксированы. Основные причины:
- Поддержание единообразия между платформами
- Предотвращение конфликтов с другими инструментами
- Упрощение запоминания для пользователей

### В: Почему формат команд отличается на разных платформах?

**О**: Это связано с техническими ограничениями каждой платформы ИИ:
- **Gemini**: Подкаталоги автоматически преобразуются в пространства имен с двоеточием.
- **Claude**: Не поддерживает подкаталоги, требуется префикс имени файла.
- **Другие платформы**: Большинство поддерживают независимые пространства имен или имеют низкий риск конфликтов.

### В: Как просмотреть все доступные команды?

**О**: Используйте следующие методы:

**Gemini CLI**:
```bash
# Перечислить все команды в пространстве имен novel
ls .gemini/commands/novel/
```

**Claude Code**:
```bash
# Перечислить все команды с префиксом novel
ls .claude/commands/ | grep ^novel\\.
```

**Другие платформы**:
```bash
# Перечислить все команды
ls .cursor/commands/
ls .windsurf/workflows/
ls .roo/commands/
```

---

## 📚 Связанные документы

- [Руководство по быстрому началу работы](../README.md)
- [Подробное описание методологии семи шагов](./workflow.md)
- [Руководство по разработке команд Gemini CLI](./gemini-command-guide.md)
- [Руководство по обновлению](./upgrade-guide.md)

---

## 🆘 Получение помощи

Если у вас возникли проблемы:

1. Проверьте [Issues проекта](https://github.com/wordflowlab/novel-writer/issues)
2. Обратитесь к [Полной документации](https://github.com/wordflowlab/novel-writer/tree/main/docs)
3. Создайте новый Issue с описанием проблемы

---

*Последнее обновление: 2025-10-25*