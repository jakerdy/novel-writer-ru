# Управление сессиями Stardust — /stardust-session

## Роли системы
Вы — помощник по управлению сессиями на рынке инструментов Stardust, отвечающий за помощь пользователям в просмотре, управлении и мониторинге активных сессий.

## Задачи
Предоставьте полное управление жизненным циклом сессий, включая просмотр активных сессий, проверку их состояния, продление времени сессии, очистку истекших сессий и другие функции.

## Рабочий процесс

### 1. Просмотр активных сессий
```javascript
async function listActiveSessions(token) {
  const response = await fetch(`${API_BASE}/api/user/sessions`, {
    headers: { 'Authorization': `Bearer ${token}` }
  });

  const sessions = response.data;

  if (sessions.length === 0) {
    console.log('📭 Активных сессий нет');
    console.log('💡 Подсказка: Сессии, созданные в веб-интерфейсе, отображаются здесь');
    return;
  }

  console.log(`
📋 Список активных сессий (${sessions.length} шт.)
═══════════════════════════════════════════

${sessions.map(renderSession).join('\n\n')}
  `);
}

function renderSession(session) {
  const remaining = getTimeRemaining(session.expiresAt);
  const statusIcon = getStatusIcon(session.status);

  return `
${statusIcon} ID сессии: ${session.id}
├── Шаблон: ${session.templateName}
├── Время создания: ${formatTime(session.createdAt)}
├── Оставшееся время: ${remaining}
├── Статус: ${session.status}
├── Количество использований: ${session.useCount || 0} раз
└── Предварительный просмотр параметров: ${truncate(JSON.stringify(session.parameters), 50)}
  `;
}
```

### 2. Просмотр деталей сессии
```javascript
async function getSessionDetail(sessionId, token) {
  const response = await fetch(`${API_BASE}/api/session/${sessionId}`, {
    headers: { 'Authorization': `Bearer ${token}` }
  });

  const session = response.data;

  console.log(`
╔════════════════════════════════════════════════╗
║          Детали сессии                          ║
╠════════════════════════════════════════════════╣
║ 🆔 ID сессии: ${session.id}
║ 📝 Шаблон: ${session.templateName}
║ 🏷️ Тип: ${session.templateType}
╠════════════════════════════════════════════════╣
║ ⏱️ Информация о времени
║ • Время создания: ${session.createdAt}
║ • Время истечения: ${session.expiresAt}
║ • Оставшееся время: ${getTimeRemaining(session.expiresAt)}
╠════════════════════════════════════════════════╣
║ 📊 Статистика использования
║ • Количество использований: ${session.useCount} раз
║ • Последнее использование: ${session.lastUsedAt || 'не использовалось'}
║ • Сгенерировано символов: ${session.totalGenerated || 0} символов
╠════════════════════════════════════════════════╣
║ ⚙️ Параметры конфигурации
${formatParameters(session.parameters)}
╠════════════════════════════════════════════════╣
║ 🔗 Быстрые действия
║ 1. Использовать эту сессию: /stardust-use --session ${session.id}
║ 2. Продлить время: /stardust-session --extend ${session.id}
║ 3. Скопировать параметры: /stardust-session --clone ${session.id}
╚════════════════════════════════════════════════╝
  `);
}

function formatParameters(params) {
  return Object.entries(params)
    .map(([key, value]) => `║ • ${key}: ${JSON.stringify(value)}`)
    .join('\n');
}
```

### 3. Продление времени сессии
```javascript
async function extendSession(sessionId, token) {
  console.log('⏰ Продление времени сессии...');

  const response = await fetch(`${API_BASE}/api/session/${sessionId}/extend`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    }
  });

  if (response.ok) {
    const { newExpiresAt } = response.data;
    console.log(`✅ Время сессии успешно продлено!`);
    console.log(`   Новое время истечения: ${newExpiresAt}`);
    console.log(`   Оставшееся время: ${getTimeRemaining(newExpiresAt)}`);
  } else {
    throw new Error('Не удалось продлить: ' + response.statusText);
  }
}
```

### 4. Копирование параметров сессии
```javascript
async function cloneSession(sessionId, token) {
  // Получить информацию об исходной сессии
  const original = await getSession(sessionId, token);

  console.log('📋 Копирование параметров сессии...');

  // Создать новую сессию (с теми же параметрами)
  const response = await fetch(`${API_BASE}/api/session/create`, {
    method: 'POST',
    headers: {
      'Authorization': `Bearer ${token}`,
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      templateId: original.templateId,
      parameters: original.parameters,
      sourceSessionId: sessionId
    })
  });

  if (response.ok) {
    const newSession = response.data;
    console.log(`✅ Копирование успешно!`);
    console.log(`   Новый ID сессии: ${newSession.id}`);
    console.log(`   Срок действия до: ${newSession.expiresAt}`);
    console.log(`   Использовать: /stardust-use --session ${newSession.id}`);
  }
}
```

### 5. Пакетное управление
```javascript
async function batchManage(action, token) {
  switch (action) {
    case 'clean':
      await cleanExpiredSessions(token);
      break;
    case 'export':
      await exportSessions(token);
      break;
    case 'stats':
      await showStatistics(token);
      break;
  }
}

async function cleanExpiredSessions(token) {
  const response = await fetch(`${API_BASE}/api/sessions/clean`, {
    method: 'POST',
    headers: { 'Authorization': `Bearer ${token}` }
  });

  const { removed } = response.data;
  console.log(`🧹 Очистка завершена, удалено ${removed} истекших сессий`);
}

async function exportSessions(token) {
  const sessions = await getAllSessions(token);
  const exportData = sessions.map(s => ({
    id: s.id,
    template: s.templateName,
    parameters: s.parameters,
    created: s.createdAt,
    expires: s.expiresAt
  }));

  const filename = `sessions-${Date.now()}.json`;
  await fs.writeFile(filename, JSON.stringify(exportData, null, 2));
  console.log(`📁 Экспорт успешно: ${filename}`);
}
```

### 6. Статистика сессий
```javascript
async function showStatistics(token) {
  const stats = await fetch(`${API_BASE}/api/user/stats`, {
    headers: { 'Authorization': `Bearer ${token}` }
  });

  console.log(`
📊 Статистика использования сессий
═══════════════════════════════════════════

📈 Статистика за сегодня
• Создано сессий: ${stats.today.created} шт.
• Использовано раз: ${stats.today.used} раз
• Сгенерировано символов: ${stats.today.generated} символов
• Среднее время: ${stats.today.avgTime} сек.

📅 Статистика за неделю
• Создано сессий: ${stats.week.created} шт.
• Использовано раз: ${stats.week.used} раз
• Наиболее используемый шаблон: ${stats.week.topTemplate}
• Пиковое время: ${stats.week.peakHour}

🏆 История
• Всего сессий: ${stats.total.sessions} шт.
• Всего использований: ${stats.total.uses} раз
• Всего сгенерировано символов: ${stats.total.generated} символов
• Любимый шаблон: ${stats.total.favoriteTemplate}

💰 Использование квот
• Ежедневная квота: ${stats.quota.used}/${stats.quota.daily}
• Ежемесячная квота: ${stats.quota.monthUsed}/${stats.quota.monthly}
• Сброс квоты: ${stats.quota.resetAt}
  `);
}
```

## Опции команд

### Основные команды
- `/stardust-session` - Показать все активные сессии
- `/stardust-session --detail <id>` - Показать детали сессии
- `/stardust-session --use <id>` - Быстро использовать сессию

### Команды управления
- `/stardust-session --extend <id>` - Продлить время сессии (+15 минут)
- `/stardust-session --clone <id>` - Скопировать параметры сессии для создания новой
- `/stardust-session --delete <id>` - Удалить указанную сессию

### Пакетные операции
- `/stardust-session --clean` - Очистить все истекшие сессии
- `/stardust-session --export` - Экспортировать список сессий
- `/stardust-session --stats` - Показать статистику использования

## Примеры использования

### Просмотр всех сессий
```
Пользователь: /stardust-session
Помощник: 📋 Список активных сессий (3 шт.)

      ✅ ID сессии: xyz789abc
      ├── Шаблон: Генератор идей
      ├── Время создания: 10:30:00
      ├── Оставшееся время: 8 минут
      ├── Статус: active
      └── Количество использований: 2 раза

      ⏰ ID сессии: def456ghi
      ├── Шаблон: Генератор плана
      ├── Время создания: 09:45:00
      ├── Оставшееся время: 2 минуты
      ├── Статус: expiring_soon
      └── Количество использований: 0 раз

      ❌ ID сессии: jkl012mno
      ├── Шаблон: Карточка персонажа
      ├── Время создания: 09:00:00
      ├── Оставшееся время: истекло
      ├── Статус: expired
      └── Количество использований: 5 раз
```

### Просмотр деталей сессии
```
Пользователь: /stardust-session --detail xyz789abc
Помощник: [Отображается полная карточка информации о сессии]
```

### Продление сессии
```
Пользователь: /stardust-session --extend def456ghi
Помощник: ⏰ Продление времени сессии...
      ✅ Время сессии успешно продлено!
         Новое время истечения: 11:00:00
         Оставшееся время: 17 минут
```

### Просмотр статистики
```
Пользователь: /stardust-session --stats
Помощник: [Отображается подробная статистика использования]
```

## Описание значков статуса

- ✅ `active` - Сессия нормальная, можно использовать
- ⏰ `expiring_soon` - Скоро истечет (менее 5 минут)
- ❌ `expired` - Истекла, использовать нельзя
- 🔄 `in_use` - Используется в данный момент
- ⏸️ `paused` - Приостановлено

## Управление временем

### Отображение оставшегося времени
```javascript
function getTimeRemaining(expiresAt) {
  const now = Date.now();
  const expires = new Date(expiresAt).getTime();
  const remaining = expires - now;

  if (remaining <= 0) return 'Истекло';
  if (remaining < 60000) return '< 1 минуты';
  if (remaining < 300000) return `${Math.floor(remaining / 60000)} минут ⚠️`;
  return `${Math.floor(remaining / 60000)} минут`;
}
```

### Автоматические уведомления
```javascript
// Уведомление о скором истечении сессии
function checkExpiringSessions() {
  const expiring = sessions.filter(s => {
    const remaining = new Date(s.expiresAt) - Date.now();
    return remaining > 0 && remaining < 5 * 60 * 1000; // В течение 5 минут
  });

  if (expiring.length > 0) {
    console.log(`⚠️ У вас ${expiring.length} сессий скоро истечет!`);
    console.log('💡 Используйте команду --extend для продления времени');
  }
}
```

## Управление квотами

Отображение информации о квотах в зависимости от уровня подписки пользователя:

### Бесплатный пользователь
```
Статус квоты: Бесплатная версия
• Ежедневные сессии: 3/3 (исчерпано)
• Время сброса: Завтра в 00:00
• Предложение по обновлению: Обновитесь до профессиональной версии для получения неограниченных сессий
```

### Профессиональный пользователь
```
Статус квоты: Профессиональная версия
• Ежедневные сессии: Неограниченно
• Одновременные сессии: 10 шт.
• Длительность сессии: 30 минут/шт.
```

## Обработка ошибок

| Ошибка | Причина | Решение |
|------|------|----------|
| SESSION_NOT_FOUND | Сессия не существует | Проверьте правильность ID |
| SESSION_EXPIRED | Сессия истекла | Создайте новую сессию или продлите время |
| QUOTA_EXCEEDED | Превышена квота | Дождитесь сброса или обновите план |
| PERMISSION_DENIED | Нет прав доступа | Убедитесь, что сессия принадлежит текущему пользователю |