```markdown
# Novel Writer - Конфигурация Gemini CLI

Этот проект настроен для поддержки Google Gemini CLI, предлагая полную систему создания романов на основе семиэтапного методологического подхода.

## 🎯 v0.10.0 Семиэтапный методологический подход

Novel Writer использует концепцию разработки, управляемой спецификациями (SDD), для создания романов через систематизированный семиэтапный процесс.

## ⚠️ Важно: Формат команд Gemini CLI

**Gemini CLI использует префикс пространства имён** `novel:`, все команды имеют формат:

```bash
/novel:имя_команды [параметры]
```

**Причина:**
- Novel Writer использует пространство имён `novel:` для предотвращения конфликтов с командами других инструментов (например, spec-kit, OpenSpec).
- Подкаталоги Gemini CLI автоматически преобразуются в пространства имён с двоеточием (путь: `.gemini/commands/novel/write.toml` → команда: `/novel:write`).

> 📖 **Подробное сопоставление команд**: См. [docs/ai-platform-commands.md](../docs/ai-platform-commands.md) для различий в форматах команд всех AI-платформ.

### Команды семиэтапного методологического подхода

1. **`/novel:constitution`** - Создание конституции (определение основных принципов).
2. **`/novel:specify`** - Спецификация истории (определение того, что нужно создать).
3. **`/novel:clarify`** - Уточнение решений (интерактивное уточнение неясных моментов).
4. **`/novel:plan`** - План создания (разработка технического решения).
5. **`/novel:tasks`** - Декомпозиция задач (генерация выполнимого списка).
6. **`/novel:write`** - Написание глав (выполнение создания контента).
7. **`/novel:analyze`** - Комплексная проверка (всесторонняя проверка качества).

### Команды управления отслеживанием

- `/novel:plot-check` - Проверка логики сюжета.
- `/novel:world-check` - Проверка согласованности мира.
- `/novel:timeline` - Управление временной шкалой.
- `/novel:relations` - Управление отношениями между персонажами.
- `/novel:track` - Комплексное отслеживание прогресса.
- `/novel:track-init` - Инициализация системы отслеживания.

### Команды экспертного режима

- `/novel:expert` - Активация экспертного режима для получения подробных инструкций.

## Использование

### Рекомендуемый рабочий процесс (семиэтапный методологический подход)

Используйте в Gemini CLI в следующей последовательности ( **обратите внимание на префикс `novel:`** ):

```bash
# 1. Установление принципов создания
> /novel:constitution

# 2. Определение спецификаций истории
> /novel:specify Фэнтезийная история о приключениях

# 3. Уточнение ключевых решений
> /novel:clarify

# 4. Разработка плана создания
> /novel:plan

# 5. Генерация списка задач
> /novel:tasks

# 6. Начало написания
> /novel:write Глава 1

# 7. Проверка качества
> /novel:analyze
```

## Разрешения инструментов

Для этого проекта настроены следующие разрешения инструментов:
- Чтение/запись файлов (read_file, write_file, edit_file).
- Выполнение команд оболочки (с ограниченным диапазоном).
- Поиск файлов (glob_files).

## Структура проекта

```
memory/           # Память для создания (новое в v0.10.0)
└── novel-constitution.md  # Конституция для создания

stories/          # Контент истории
├── [Имя истории]/
│   ├── specification.md   # Спецификация истории (заменяет story.md)
│   ├── clarification.md   # Запись уточнений (новое в v0.10.0)
│   ├── creative-plan.md   # План создания (заменяет outline.md)
│   ├── tasks.md           # Список задач (новое в v0.10.0)
│   ├── analysis-report.md # Отчет об анализе (новое в v0.10.0)
│   └── content/           # Содержание глав (заменяет chapters/)

spec/             # Конфигурация и база знаний
├── tracking/     # Отслеживание прогресса
├── knowledge/    # Настройки мира
└── presets/      # Шаблоны методов написания

.gemini/          # Конфигурация Gemini
├── commands/     # Определение команд (TOML)
└── settings.json # Настройки Gemini
```

## Основные концепции методологии

**Создание, управляемое спецификациями** : больше не полагайтесь на вдохновение и случайность, а используйте точные определения спецификаций для управления генерацией контента.

- **Ограничения уровня конституции**: Принципы создания являются высшими правилами, которые нельзя нарушать.
- **Спецификация как требование**: Определяйте историю, как менеджер продукта пишет PRD.
- **План как путь**: Техническое решение определяет, как реализовать спецификацию.
- **Задача как исполнение**: Отслеживаемые, проверяемые единицы исполнения.
- **Непрерывная проверка**: Качество проверяется на каждом этапе.

## Поддержка плагинов

Если установлены плагины, будут доступны дополнительные команды. Просмотрите каталог `plugins/`, чтобы узнать об установленных плагинах.

## Примечания

1. **Следуйте семиэтапному процессу**: Выполняйте команды в порядке методологии, не пропускайте шаги.
2. **Сначала определение, затем исполнение**: Сначала завершите определение спецификаций, затем начинайте писать.
3. **Непрерывная проверка**: Запускайте `/analyze` каждые 5 глав для проверки качества.
4. **Итеративная оптимизация**: Корректируйте спецификации и планы на основе результатов анализа.

## Получение помощи

- Используйте `/expert` для активации экспертного режима и получения подробных инструкций.
- Обратитесь к каталогу `docs/` для получения подробной документации.
- Посетите репозиторий проекта: https://github.com/wordflowlab/novel-writer

## Известные проблемы и решения

### Проблема с китайскими символами
Gemini CLI может иногда выводить отдельные китайские символы с ошибками (отображаются как  или другие искажения), это известная проблема кодировки Gemini.

#### Меры предосторожности
1. **Настройки терминала**
   - Windows: Используйте Windows Terminal или PowerShell (избегайте cmd).
   - Mac/Linux: Убедитесь, что терминал поддерживает UTF-8.

2. **Настройка переменных окружения**
   ```bash
   # Windows PowerShell
   [Console]::OutputEncoding = [System.Text.Encoding]::UTF8

   # Mac/Linux
   export LANG=zh_CN.UTF-8
   export LC_ALL=zh_CN.UTF-8
   ```

#### Решение при появлении искаженных символов
1. **Повторная генерация**: Повторно выполните ту же команду, обычно второй раз она работает нормально.
2. **Ручное исправление**: Отредактируйте сгенерированный файл напрямую и замените искаженные символы правильными китайскими.
3. **Поэтапная обработка**: Если в каком-то разделе появляются искаженные символы, перегенерируйте только этот раздел.

#### Распространенные шаблоны искажения
- `` → Обычно это часто используемые слова, такие как "的", "了".
- `\u4e2d\u6587` → Unicode-экранирование, необходимо преобразовать обратно в китайский.
- Некоторые знаки препинания отображаются некорректно → Вручную замените китайскими знаками препинания.

#### Временное решение
Если искажения появляются часто, вы можете:
1. Генерировать более короткие абзацы (уменьшить объем вывода за один раз).
2. Использовать стандартный режим, а не режим по разделам.
3. Проверять и исправлять сразу после генерации.

Примечание: Google работает над решением этой проблемы, и последующие версии должны улучшить ситуацию.

---
*Этот проект разработан командой Novel Writer и предназначен для создания китайских романов с использованием ИИ.*
```