```bash
#!/usr/bin/env bash
# Скрипт помощника по методам интеллектуального письма
# Для команды /method

set -e

# Получить корневой каталог проекта
PROJECT_ROOT="$(pwd)"
CONFIG_FILE="${PROJECT_ROOT}/.specify/config.json"

# Проверить файл конфигурации
if [ ! -f "$CONFIG_FILE" ]; then
    echo "❌ Файл конфигурации проекта не найден"
    echo "Убедитесь, что вы запускаете эту команду в каталоге проекта романа"
    exit 1
fi

# Прочитать текущий метод
CURRENT_METHOD=$(grep -o '"method"[[:space:]]*:[[:space:]]*"[^"]*"' "$CONFIG_FILE" | sed 's/.*"\([^"]*\)"$/\1/')

# Вывести информацию для использования ИИ
echo "📚 Помощник по методам письма запущен"
echo "Текущий метод: ${CURRENT_METHOD:-three-act}"
echo ""
echo "Доступные методы письма:"
echo "- three-act: Трёхактная структура"
echo "- hero-journey: Путешествие героя"
echo "- story-circle: Круг историй"
echo "- seven-point: Семиточечная структура"
echo "- pixar-formula: Формула Пиксар"
echo "- snowflake: Метод снежинки"
echo ""
echo "Интерфейс ИИ готов, пожалуйста, узнайте потребности пользователя через диалог"

# Успешный выход
exit 0
```