```markdown
# Руководство по использованию команд платформы AI

В этом документе подробно описаны команды с косой чертой для Novel Writer на различных платформах AI.

## 📋 Сводная таблица

| Платформа AI | Формат команды | Пример | Пространство имен |
|---------|---------|------|----------|
| **Gemini CLI** | `/novel:имя_команды` | `/novel:write` | `novel:` |
| **Claude Code** | `/novel.имя_команды` | `/novel.write` | `novel.` |
| **Cursor** | `/имя_команды` | `/write` | Нет |
| **Windsurf** | `/имя_команды` | `/write` | Нет |
| **Roo Code** | `/имя_команды` | `/write` | Нет |
| **GitHub Copilot** | `/имя_команды` | `/write` | Нет |
| **Qwen Code** | `/имя_команды` | `/write` | Нет |
| **OpenCode** | `/имя_команды` | `/write` | Нет |
| **Codex CLI** | `/novel-имя_команды` | `/novel-write` | `novel-` |
| **Kilo Code** | `/имя_команды` | `/write` | Нет |
| **Auggie CLI** | `/имя_команды` | `/write` | Нет |
| **CodeBuddy** | `/имя_команды` | `/write` | Нет |
| **Amazon Q** | `/имя_команды` | `/write` | Нет |

## 🎯 Зачем использовать пространства имен?

Novel Writer использует префиксы пространств имен (namespace) на некоторых платформах AI, в основном по следующим причинам:

1. **Избежание конфликтов команд**: предотвращение совпадения имен команд с другими инструментами (например, spec-kit, OpenSpec и т. д.).
2. **Четкое указание принадлежности команды**: позволяет пользователям точно знать, что это команда, предоставленная Novel Writer.
3. **Ограничения платформы**: некоторые платформы не поддерживают организацию подкаталогов и требуют использования префиксов для различения.

### Правила пространства имен

- **Gemini CLI**: использует пространство имен подкаталога `novel/`, формат команды: `/novel:имя_команды`.
  - Причина: подкаталоги Gemini автоматически преобразуются в пространства имен с двоеточием.
  - Путь: `.gemini/commands/novel/write.toml` → `/novel:write`

- **Claude Code**: использует префикс имени файла `novel.`, формат команды: `/novel.имя_команды`.
  - Причина: Claude не поддерживает пространства имен подкаталогов.
  - Путь: `.claude/commands/novel.write.md` → `/novel.write`

- **Codex CLI**: использует префикс имени файла `novel-`, формат команды: `/novel-имя_команды`.
  - Причина: запросы Codex не поддерживают подкаталоги, использование префикса с дефисом облегчает идентификацию.
  - Путь: `.codex/prompts/novel-write.md` → `/novel-write`

- **Другие платформы**: без пространства имен, используется непосредственно имя команды.
  - Причина: эти платформы обычно являются средами независимых проектов с низким риском конфликтов.

---

## 🚀 Справочник команд Gemini CLI

### Формат команды
```bash
/novel:имя_команды [параметры]
```

### Команды методологии семи шагов

#### 1. Создание конституции
```bash
/novel:constitution
```
Определение основных принципов творчества, установление высших руководящих принципов для создания контента.

#### 2. Спецификация истории
```bash
/novel:specify [описание истории]
```
Четкое определение того, что нужно создать, определение истории, как в документе с требованиями к продукту.

Пример:
```bash
/novel:specify Фэнтезийный роман о совершенствовании бессмертных
```

#### 3. Уточнение решений
```bash
/novel:clarify
```
Интерактивное уточнение ключевых точек принятия решений, прояснение неясных моментов.

#### 4. Планирование создания
```bash
/novel:plan
```
Разработка технического плана, определение того, как реализовать спецификацию истории.

#### 5. Декомпозиция задач
```bash
/novel:tasks
```
Создание выполнимого списка задач, систематическое продвижение процесса создания.

#### 6. Написание глав
```bash
/novel:write [описание главы]
```
Выполнение написания главы на основе списка задач, автоматическая загрузка контекста.

Пример:
```bash
/novel:write Глава 1
/novel:write Главный герой впервые входит в мир совершенствования бессмертных
```

#### 7. Комплексная проверка
```bash
/novel:analyze
```
Всесторонняя проверка качества, обеспечение единообразия качества создания контента.

### Команды отслеживания и управления

#### Инициализация системы отслеживания
```bash
/novel:track-init
```
Инициализация систем отслеживания персонажей, сюжета, временной шкалы и т. д.

#### Комплексное отслеживание
```bash
/novel:track
```
Комплексное отслеживание прогресса и интеллектуальный анализ, просмотр статуса создания.

#### Проверка сюжета
```bash
/novel:plot-check
```
Проверка логической согласованности сюжета, выявление потенциальных проблем.

#### Управление временной шкалой
```bash
/novel:timeline
```
Управление и проверка временной шкалы истории.

#### Управление отношениями между персонажами
```bash
/novel:relations
```
Отслеживание и обновление сети отношений между персонажами.

#### Проверка мировоззрения
```bash
/novel:world-check
```
Проверка согласованности настроек мировоззрения.

### Команды экспертного режима

#### Активация эксперта
```bash
/novel:expert [тип эксперта]
```
Получение углубленных рекомендаций в конкретной области.

Пример:
```bash
/novel:expert plot      # Эксперт по структуре сюжета
/novel:expert character # Эксперт по созданию персонажей
/novel:expert style     # Эксперт по стилистической доработке
```

### Другие вспомогательные команды

#### Чек-лист
```bash
/novel:checklist
```
Создание чек-листа перед написанием.

---

## 🔧 Справочник команд Claude Code

### Формат команды
```bash
/novel.имя_команды [параметры]
```

### Основной список команд

Все команды имеют ту же функциональность, что и в Gemini CLI, просто замените `/novel:` на `/novel.`:

```bash
# Методология семи шагов
/novel.constitution
/novel.specify [описание истории]
/novel.clarify
/novel.plan
/novel.tasks
/novel.write [описание главы]
/novel.analyze

# Отслеживание и управление
/novel.track-init
/novel.track
/novel.plot-check
/novel.timeline
/novel.relations
/novel.world-check

# Экспертный режим
/novel.expert [тип эксперта]

# Другое
/novel.checklist
```

---

## 📟 Справочник команд Codex CLI

### Формат команды

```bash
/novel-имя_команды [параметры]
```

### Основной список команд

Все команды имеют ту же функциональность, что и в Gemini CLI, просто замените `/novel:` на `/novel-`:

```bash
# Методология семи шагов
/novel-constitution
/novel-specify [описание истории]
/novel-clarify
/novel-plan
/novel-tasks
/novel-write [описание главы]
/novel-analyze

# Отслеживание и управление
/novel-track-init
/novel-track
/novel-plot-check
/novel-timeline
/novel-relations
/novel-world-check

# Экспертный режим
/novel-expert [тип эксперта]

# Другое
/novel-checklist
```

### Особые примечания

**Поддержка Codex CLI** (v0.19.0+):
- ✅ Использование чистого формата Markdown (без YAML frontmatter)
- ✅ Файлы команд расположены в каталоге `.codex/prompts/`
- ✅ Использование префикса `novel-` для предотвращения конфликтов с другими инструментами
- ✅ Полная поддержка всех функций команд Novel Writer

**Способ установки**:
```bash
novel init my-novel --ai codex
```

**Примечания**:
- Функция пользовательских запросов Codex CLI требует, чтобы файлы команд были в чистом формате Markdown.
- Используйте дефис `-` вместо точки `.` или двоеточия `:` в качестве разделителя пространства имен.
- Все выполнение скриптов обрабатывается AI автоматически, ручная настройка не требуется.

---

## 💡 Справочник команд для других платформ AI

### Формат команды
```bash
/имя_команды [параметры]
```

### Основной список команд

Платформы Cursor, Windsurf, Roo Code, GitHub Copilot, Qwen Code, OpenCode, Codex CLI, Kilo Code, Auggie CLI, CodeBuddy, Amazon Q и другие используют один и тот же формат команды, без префикса:

```bash
# Методология семи шагов
/constitution
/specify [описание истории]
/clarify
/plan
/tasks
/write [описание главы]
/analyze

# Отслеживание и управление
/track-init
/track
/plot-check
/timeline
/relations
/world-check

# Экспертный режим
/expert [тип эксперта]

# Другое
/checklist
```

---

## 📖 Пример использования

### Полный рабочий процесс Gemini CLI

```bash
# 1. Установить принципы создания
> /novel:constitution

# 2. Определить спецификацию истории
> /novel:specify Роман-триллер о путешествиях во времени с элементами саспенса

# 3. Уточнить ключевые решения
> /novel:clarify

# 4. Спланировать создание
> /novel:plan

# 5. Сгенерировать список задач
> /novel:tasks

# 6. Начать написание
> /novel:write Глава 1: Временной сдвиг

# 7. Регулярно проверять качество
> /novel:analyze

# Отслеживание и управление
> /novel:track
> /novel:timeline
```

### Полный рабочий процесс Claude Code

```bash
# Используйте тот же процесс, просто замените префикс команды
> /novel.constitution
> /novel.specify Роман-триллер о путешествиях во времени с элементами саспенса
> /novel.clarify
> /novel.plan
> /novel.tasks
> /novel.write Глава 1: Временной сдвиг
> /novel.analyze
```

### Полный рабочий процесс Codex CLI

```bash
# Используйте префикс с дефисом
> /novel-constitution
> /novel-specify Роман-триллер о путешествиях во времени с элементами саспенса
> /novel-clarify
> /novel-plan
> /novel-tasks
> /novel-write Глава 1: Временной сдвиг
> /novel-analyze
```

### Полный рабочий процесс Cursor

```bash
# Префикс не требуется, используйте непосредственно имя команды
> /constitution
> /specify Роман-триллер о путешествиях во времени с элементами саспенса
> /clarify
> /plan
> /tasks
> /write Глава 1: Временной сдвиг
> /analyze
```

---

## 🔍 Часто задаваемые вопросы

### В: Почему моя команда не работает?

**О**: Пожалуйста, проверьте следующие пункты:

1. **Подтвердите формат команды**:
   - Gemini CLI использует `/novel:имя_команды`
   - Claude Code использует `/novel.имя_команды`
   - Другие платформы используют `/имя_команды`

2. **Проверьте наличие файла команды**:
   ```bash
   # Gemini
   ls .gemini/commands/novel/

   # Claude
   ls .claude/commands/ | grep novel.

   # Cursor
   ls .cursor/commands/
   ```

3. **Убедитесь, что проект инициализирован**:
   ```bash
   ls .specify/config.json
   ```

### В: Можно ли настроить префикс команды?

**О**: В настоящее время пространства имен фиксированы, в основном по следующим причинам:
- Поддержание единообразия между платформами.
- Предотвращение конфликтов с другими инструментами.
- Упрощение запоминания для пользователей.

### В: Почему формат команд отличается на разных платформах?

**О**: Это связано с техническими ограничениями каждой платформы AI:
- **Gemini**: подкаталоги автоматически преобразуются в пространства имен с двоеточием.
- **Claude**: не поддерживает подкаталоги, требуется префикс имени файла.
- **Другие платформы**: большинство поддерживают независимые пространства имен или имеют низкий риск конфликтов.

### В: Как просмотреть все доступные команды?

**О**: Используйте следующие методы:

**Gemini CLI**:
```bash
# Перечислить все команды в пространстве имен novel
ls .gemini/commands/novel/
```

**Claude Code**:
```bash
# Перечислить все команды с префиксом novel.
ls .claude/commands/ | grep ^novel\\.
```

**Другие платформы**:
```bash
# Перечислить все команды
ls .cursor/commands/
ls .windsurf/workflows/
ls .roo/commands/
```

---

## 📚 Связанные документы

- [Руководство по быстрому началу работы](../README.md)
- [Подробное описание методологии семи шагов](./workflow.md)
- [Руководство по разработке Gemini CLI](./gemini-command-guide.md)
- [Руководство по обновлению](./upgrade-guide.md)

---

## 🆘 Получить помощь

Если у вас возникли проблемы:

1. Просмотрите [Issues проекта](https://github.com/wordflowlab/novel-writer/issues).
2. Обратитесь к [полной документации](https://github.com/wordflowlab/novel-writer/tree/main/docs).
3. Создайте новый Issue с описанием проблемы.

---

*Последнее обновление: 25.10.2025*
```