# Руководство по обновлению Novel Writer

> Как обновить существующий проект до последней версии

## Сценарии использования

- ✅ Существующие проекты созданы с использованием старой версии novel-writer-cn
- ✅ Желание использовать функции новой версии (улучшения Claude Code, управление несколькими потоками и т. д.)
- ✅ Нежелание терять уже написанный контент и пользовательские настройки

## Совместимость версий

| Старая версия | Новая версия | Совместимость | Рекомендуемые действия |
|---|---|---|---|
| 0.11.x | 0.12.x | ✅ Совместимо | Просто обновите файлы команд |
| 0.10.x | 0.12.x | ⚠️ Частично совместимо | Требуется миграция структуры данных |
| < 0.10.0 | 0.12.x | ❌ Несовместимо | Рекомендуется пересоздать проект |

## Методы обновления

### Метод 1: Автоматическое обновление (рекомендуется, самый простой)

**Применимо, если**：
- Желание автоматизировать процесс обновления
- Автоматическое резервное копирование, безопасно и надежно
- Сохранение всех пользовательских данных и настроек

**Шаги**：

```bash
# 1. Обновите глобально установленный novel-writer-cn
npm install -g novel-writer-cn@latest

# 2. Перейдите в каталог проекта и выполните команду обновления
cd my-novel
novel upgrade

# Или укажите конкретную AI-платформу
novel upgrade --ai claude
```

**Описание**：
- ✅ Автоматическое резервное копирование: перед обновлением автоматически создается резервная копия (каталог backup/)
- ✅ Сохранение контента: весь контент (stories/, spec/, memory/) полностью сохраняется
- ✅ Обновление команд: обновление файлов команд (с пространствами имен) из сборочных артефактов
- ✅ Обновление скриптов: синхронизация последних вспомогательных скриптов
- ⚠️ Возможность отката: в случае проблем можно восстановить из каталога backup/

**Описание пространств имен** (v0.15.0+):
- Файлы команд Claude используют префикс `novel.*` (например, `novel.specify.md`), чтобы избежать конфликтов с другими инструментами
- Файлы команд Gemini используют подкаталог `novel/`
- При использовании по-прежнему будут доступны `/specify`, `/write` и т. д., пространство имен прозрачно для пользователя

#### Новые опции обновления в v0.16.5

**Интерактивный выбор**：
```bash
# Выберите, что обновлять, через интерактивный интерфейс
novel upgrade -i
```
Появится интерфейс выбора с флажками, по умолчанию выбраны "Файлы команд" и "Файлы скриптов".

**Выборочное обновление**：
```bash
# Обновить только файлы команд (скрипты не обновлять)
novel upgrade --commands

# Обновить только файлы скриптов
novel upgrade --scripts

# Обновить файлы шаблонов (используется при обновлении фреймворка)
novel upgrade --templates

# Обновить файлы памяти (личные корпуса и т. д.)
novel upgrade --memory

# Комбинированное использование: обновить команды и скрипты
novel upgrade --commands --scripts
```

**Указание платформы**：
```bash
# Обновить только для Claude
novel upgrade --ai claude

# Обновить файлы команд для Gemini
novel upgrade --ai gemini --commands

# Обновить все установленные платформы (поведение по умолчанию)
novel upgrade
# Или явно указать
novel upgrade --all
```

**Быстрые опции**：
```bash
# Пропустить подтверждение (автоматическое выполнение)
novel upgrade -y

# Пропустить резервное копирование (быстрое обновление, использовать с осторожностью)
novel upgrade --no-backup

# Предварительный просмотр содержимого обновления (без фактического изменения)
novel upgrade --dry-run

# Комбинированное использование: быстрое обновление без резервного копирования
novel upgrade -y --no-backup
```

**Примеры практического использования**：

1. **Первое обновление до v0.16.5** (рекомендуется):
   ```bash
   novel upgrade -i
   ```
   Интерактивный выбор обновляемого контента, безопасно и контролируемо.

2. **Ежедневное обновление команд**：
   ```bash
   novel upgrade --commands
   ```
   Обновляются только файлы команд, скрипты остаются стабильными.

3. **Полное тестирование обновления**：
   ```bash
   novel upgrade --dry-run
   ```
   Сначала просмотрите содержимое обновления, затем выполните.

4. **Срочное быстрое исправление**：
   ```bash
   novel upgrade --ai claude --commands -y
   ```
   Быстрое обновление команд Claude, пропуск подтверждения.

5. **Обслуживание проектов с несколькими платформами**：
   ```bash
   # Обновить команды и скрипты для всех платформ
   novel upgrade --commands --scripts
   ```

**Пример отчета об обновлении**：
```
📦 Обновление проекта Novel Writer

Текущая версия: 0.16.4
Целевая версия: 0.16.5

✓ Обнаружены настройки AI: Claude Code, Cursor, Gemini CLI

Цели обновления: Claude Code, Cursor, Gemini CLI

Обновляемый контент: Файлы команд, Файлы скриптов

📦 Создание резервной копии...
  ✓ Резервное копирование .claude/
  ✓ Резервное копирование .cursor/
  ✓ Резервное копирование .gemini/
  ✓ Резервное копирование .specify/scripts/
✓ Резервное копирование завершено: backup/2025-10-12T15-30-45

📝 Обновление файлов команд...
  ✓ Claude Code: 14 файлов
  ✓ Cursor: 14 файлов
  ✓ Gemini CLI: 14 файлов

🔧 Обновление файлов скриптов...
  ✓ Обновлено 8 скриптов bash
  ✓ Обновлено 8 скриптов powershell

📊 Отчет об обновлении

✅ Обновление завершено!

Статистика обновления:
  • Версия: 0.16.4 → 0.16.5
  • AI платформы: Claude Code, Cursor, Gemini CLI
  • Файлы команд: 42 файла
  • Файлы скриптов: 16 файлов

📦 Местоположение резервной копии: backup/2025-10-12T15-30-45
   Если требуется откат, удалите текущие файлы и восстановите из резервной копии

✨ Подсказка о новых функциях:
  • Управление температурой AI: команда write.md получила новые инструкции по усилению творчества
  • Поддержка нескольких платформ: обновлены команды для всех 13 AI платформ
  • Интеллектуальный анализ: улучшенная проверка качества команды /analyze

📚 Просмотреть подробное руководство по обновлению: docs/upgrade-guide.md
   Или посетите: https://github.com/wordflowlab/novel-writer/blob/main/docs/upgrade-guide.md
```

---

### Метод 2: Повторная инициализация с помощью команды init

**Применимо, если**：
- Требуется полная сброс настроек
- Нет пользовательских файлов команд
- Допустимо полное перезаписывание файлов команд

**Шаги**：

```bash
# 1. Обновите глобально установленный novel-writer-cn
npm install -g novel-writer-cn@latest

# 2. Перейдите в каталог проекта
cd my-novel

# 3. Резервное копирование (опционально, но настоятельно рекомендуется)
cp -r .claude .claude.backup
cp -r .specify .specify.backup

# 4. Повторная инициализация (обновляет только файлы команд и скрипты)
novel init --here --ai claude

# 5. Проверка обновления
ls .claude/commands/  # Должны отображаться все обновленные файлы команд
```

**Примечания**：
- ✅ Сохраняется: stories/, spec/, memory/ и другие пользовательские данные
- ✅ Обновляется: .claude/commands/, .specify/scripts/ и другие системные файлы
- ⚠️ Перезаписывается: если вы изменяли файлы команд, потребуется вручную объединить изменения

---

### Метод 3: Ручное копирование файлов команд (для опытных пользователей)

**Применимо, если**：
- Были изменены файлы команд, требуется точный контроль
- Желание полностью контролировать процесс обновления
- Требуется сохранить пользовательские настройки

**Шаги**：

#### 3.1 Найдите место установки novel-writer-cn

```bash
# macOS/Linux
npm list -g novel-writer-cn
# Пример вывода: /usr/local/lib/node_modules/novel-writer-cn

# Windows
npm list -g novel-writer-cn
# Пример вывода: C:\Users\YourName\AppData\Roaming\npm\node_modules\novel-writer-cn
```

#### 3.2 Скопируйте сборочные артефакты

```bash
# macOS/Linux
cd my-novel
cp -r /usr/local/lib/node_modules/novel-writer-cn/dist/claude/.claude/commands/* .claude/commands/

# Windows (PowerShell)
cd my-novel
Copy-Item -Recurse "C:\Users\YourName\AppData\Roaming\npm\node_modules\novel-writer-cn\dist\claude\.claude\commands\*" .claude\commands\
```

#### 3.3 Обновите файлы скриптов

```bash
# macOS/Linux
cp -r /usr/local/lib/node_modules/novel-writer-cn/scripts/* .specify/scripts/

# Windows (PowerShell)
Copy-Item -Recurse "C:\Users\YourName\AppData\Roaming\npm\node_modules\novel-writer-cn\scripts\*" .specify\scripts\
```

#### 3.4 Проверка обновления

```bash
# Проверьте количество файлов команд (должно быть 14)
ls .claude/commands/ | wc -l

# Проверьте префикс пространства имен (Claude использует префикс novel.*)
ls .claude/commands/novel.*

# Проверьте поля расширенной версии (должны отображаться argument-hint, allowed-tools и т. д.)
head -20 .claude/commands/novel.analyze.md
```

---

## Поддержка обновления нескольких платформ в v0.16.5

### Поддерживаемые AI-платформы (13 шт.)

Novel Writer v0.16.5 полностью поддерживает обновление следующих 13 платформ AI:

| Платформа | Идентификатор | Каталог команд | Особые примечания |
|---|---|---|---|
| **Claude Code** | `claude` | `.claude/commands` | Платформа по умолчанию, формат Markdown |
| **Cursor** | `cursor` | `.cursor/commands` | Формат Markdown |
| **Gemini CLI** | `gemini` | `.gemini/commands` | Формат TOML, требуется settings.json |
| **Windsurf** | `windsurf` | `.windsurf/workflows` | Использует каталог workflows (не commands) |
| **Roo Code** | `roocode` | `.roo/commands` | Формат Markdown |
| **GitHub Copilot** | `copilot` | `.github/prompts` | Дополнительно включает конфигурацию каталога .vscode |
| **Qwen Code** | `qwen` | `.qwen/commands` | Формат Markdown |
| **OpenCode** | `opencode` | `.opencode/command` | Использует command (единственное число) |
| **Codex CLI** | `codex` | `.codex/prompts` | Формат Markdown |
| **Kilo Code** | `kilocode` | `.kilocode/workflows` | Использует каталог workflows |
| **Auggie CLI** | `auggie` | `.augment/commands` | Формат Markdown |
| **CodeBuddy** | `codebuddy` | `.codebuddy/commands` | Формат Markdown |
| **Amazon Q** | `q` | `.amazonq/prompts` | Формат Markdown |

### Примеры обновления нескольких платформ

#### Обновление одной платформы

```bash
# Обновить Claude Code
novel upgrade --ai claude

# Обновить Gemini CLI
novel upgrade --ai gemini

# Обновить Windsurf
novel upgrade --ai windsurf

# Обновить GitHub Copilot (включая конфигурацию .vscode)
novel upgrade --ai copilot
```

#### Пакетное обновление нескольких платформ

```bash
# Обновить все установленные платформы (автоматическое определение)
novel upgrade

# Или явно указать обновление всех платформ
novel upgrade --all

# Просмотреть платформы, которые будут обновлены (режим предварительного просмотра)
novel upgrade --dry-run
```

#### Обновление специфичных функций платформы

```bash
# Обновить только файлы команд для Claude
novel upgrade --ai claude --commands

# Обновить только скрипты для Gemini
novel upgrade --ai gemini --scripts

# Обновить команды и шаблоны для Windsurf
novel upgrade --ai windsurf --commands --templates

# Быстрое обновление Cursor (пропустить подтверждение)
novel upgrade --ai cursor -y
```

### Механизм определения платформы

`novel upgrade` автоматически определяет установленные в проекте AI-платформы:

```bash
$ novel upgrade

📦 Обновление проекта Novel Writer

Текущая версия: 0.16.4
Целевая версия: 0.16.5

✓ Обнаружены настройки AI: Claude Code, Cursor, Gemini CLI

Цели обновления: Claude Code, Cursor, Gemini CLI
```

Если в проекте установлено несколько платформ, по умолчанию будут обновлены все установленные платформы.

### Особая обработка платформ

#### Windsurf и Kilo Code
Эти платформы используют каталог `workflows` вместо `commands`. Команда обновления автоматически обрабатывает это:

```bash
novel upgrade --ai windsurf
# Фактически обновляется: .windsurf/workflows/
```

#### GitHub Copilot
GitHub Copilot требует дополнительных конфигурационных файлов VSCode, которые также будут обновлены:

```bash
novel upgrade --ai copilot
# Фактически обновляется:
#   - .github/prompts/
#   - .vscode/settings.json
```

#### OpenCode
OpenCode использует каталог `command` в единственном числе:

```bash
novel upgrade --ai opencode
# Фактически обновляется: .opencode/command/
```

#### Gemini CLI
Gemini использует формат TOML, и при обновлении команды будут автоматически преобразованы:

```bash
novel upgrade --ai gemini
# Формат файла команды: TOML
# Дополнительные файлы: .gemini/settings.json, GEMINI.md
```

### Проекты со смешанными платформами

Если в вашем проекте установлено несколько AI-платформ (например, Claude и Cursor одновременно), вы можете:

1. **Обновить все** (рекомендуется):
   ```bash
   novel upgrade
   ```

2. **Выборочное обновление**:
   ```bash
   # Обновить только Claude
   novel upgrade --ai claude

   # Интерактивный выбор платформ и контента
   novel upgrade -i
   ```

3. **Поэтапное обновление**:
   ```bash
   # Сначала обновить основную платформу
   novel upgrade --ai claude

   # После успешного тестирования обновить другие платформы
   novel upgrade --ai cursor
   novel upgrade --ai gemini
   ```

---

## Руководство по обновлению новых функций в v0.12.2+

### Система сборки и архитектура пространств имен (v0.15.0)

**Новые возможности**：
- Система сборки на основе spec-kit
- Изоляция пространств имен (Claude: префикс `novel.*`, Gemini: подкаталог `novel/`)
- Единое управление исходными файлами (templates/commands/)
- Генерация команд для нескольких платформ во время сборки (каталог dist/)

**Как включить**：

1. **Автоматическое обновление** (рекомендуется):
   ```bash
   novel upgrade
   ```
   Автоматически обновится до новой версии с пространствами имен

2. **Повторная инициализация**:
   ```bash
   novel init --here --ai claude
   ```
   Все команды автоматически будут использовать версию с пространством имен

3. **Проверка пространств имен**:
   ```bash
   # Команды Claude должны иметь префикс novel.*
   ls .claude/commands/novel.*

   # Команды Gemini должны находиться в подкаталоге novel/
   ls .gemini/commands/novel/
   ```

### Улучшения Claude Code (v0.12.2)

**Новые возможности**：
- Все 14 команд имеют версию с улучшениями Claude Code
- Интеллектуальные подсказки параметров (argument-hint)
- Детальный контроль разрешений (allowed-tools)
- Динамическая загрузка контекста (встроенное выполнение bash)

**Как включить**：

Используйте `novel upgrade` или `novel init` для автоматического получения улучшенных версий

**Проверка улучшенных функций**：
```bash
# Проверка наличия полей расширения (обратите внимание на префикс пространства имен)
grep "argument-hint" .claude/commands/novel.analyze.md
grep "allowed-tools" .claude/commands/novel.track.md
grep "model:" .claude/commands/novel.write.md
```

### Система управления несколькими сюжетными линиями (v0.12.0)

**Новые возможности**:
- specification.md, глава 5 «Спецификация управления сюжетными линиями»
- plot-tracker.json поддерживает отслеживание нескольких сюжетных линий
- creative-plan.md, аннотации глав и абзацев для активных сюжетных линий

**Шаги обновления**:

1. **Если specification.md еще не создан**:
   - Запустите команду `/specify` в Claude/Cursor
   - Новая версия автоматически включит главу 5

2. **Если specification.md уже существует** (требуется ручное добавление):
   - Откройте `stories/your-story/specification.md`
   - Вставьте следующий контент между главами 5 и 6:

```markdown
## Пять. Спецификация управления сюжетными линиями

> **Инструкции по управлению несколькими сюжетными линиями**: Эта глава определяет все сюжетные линии (основные, второстепенные) и стратегии их управления для истории.

### 5.1 Таблица определения сюжетных линий

| ID линии | Название линии | Тип | Приоритет | Главы начала/конца | Основной конфликт | Основные персонажи |
|---|---|---|---|---|---|---|
| PL-01 | [Название линии, например, «Основная линия»] | Основная линия | P0 | 1-100 | [Основной конфликт] | [Список персонажей] |

### 5.2 Планирование темпа сюжетной линии

| ID линии | Том 1 | Том 2 | Том 3 |
|---|---|---|---|
| PL-01 | ⭐⭐⭐ Активно | ⭐⭐ Средне | ⭐⭐⭐ Активно |

### 5.3 Планирование точек пересечения сюжетных линий

| ID точки пересечения | Глава | Задействованные линии | Содержание пересечения | Ожидаемый эффект |
|---|---|---|---|---|
| X-001 | [Номер главы] | PL-01+PL-02 | [Описание пересечения] | [Эффект] |

### 5.4 Таблица управления завязками

| ID завязки | Глава завязки | Задействованные линии | Содержание завязки | Глава раскрытия | Способ раскрытия |
|---|---|---|---|---|---|
| F-001 | [Глава] | PL-01 | [Содержание] | [Глава] | [Способ] |

### 5.5 Матрица принятия решений по изменению сюжетной линии

**Чек-лист изменений**:
1. Проверьте раздел 5.2: В каких томах активна эта линия?
2. Проверьте раздел 5.3: Какие точки пересечения задействует эта линия?
3. Проверьте раздел 5.4: Какие завязки задействует эта линия?
4. Проверьте creative-plan.md: Какие главы/абзацы требуют синхронного изменения?

### 5.6 Принципы согласованности сюжетных линий

- Каждая сюжетная линия должна иметь четкое начало, развитие и завершение.
- Основная линия занимает 40-60% общего объема.
- Не более 2-3 второстепенных линий.
```

3. **Переинициализация системы отслеживания** (если уже использовалась):
   ```bash
   # Запустите в AI-помощнике
   /track-init
   ```
   Это перегенерирует plot-tracker.json на основе новой спецификации управления сюжетными линиями.

---

## Часто задаваемые вопросы FAQ

### В1: Потеряется ли содержимое моих глав после обновления?

**О**: Нет. Обновление затрагивает только файлы команд (`.claude/commands/`) и скрипты (`.specify/scripts/`), не затрагивая ваше творческое содержимое:
- ✅ Полностью сохранено: каталог `stories/` (все главы)
- ✅ Полностью сохранено: каталог `spec/` (спецификации, планы, данные отслеживания)
- ✅ Полностью сохранено: каталог `memory/` (конституция, личные корпуса текстов)

### В2: Я изменил файлы команд, обновление их перезапишет?

**О**: Да, `novel upgrade` или `novel init --here` перезапишут файлы команд. Рекомендуется:

**Обновление v0.15.0+ (с пространствами имен)**:
1. Сначала сделайте резервную копию: `cp -r .claude .claude.backup`
2. Запустите `novel upgrade`
3. Сравните различия: `diff .claude.backup/commands/analyze.md .claude/commands/novel.analyze.md`
   - Обратите внимание: новые имена файлов имеют префикс `novel.`
4. Вручную объедините пользовательские части в новые файлы

Или используйте **Метод 3 (ручное копирование)**, копируя только нужные команды.

### В3: Функции расширения Claude Code не работают после обновления?

**О**: Проверьте шаги:
1. Убедитесь, что вы используете Claude Code (а не Cursor/Windsurf).
2. Проверьте, есть ли в файлах команд поля расширения (обратите внимание на префикс пространства имен):
   ```bash
   grep "argument-hint\|allowed-tools\|model:" .claude/commands/novel.analyze.md
   ```
3. Если полей нет, запустите `novel upgrade` для повторного обновления.

### В4: Как узнать, какие команды получили расширение Claude Code?

**О**: В v0.12.2 все 14 команд имеют расширенные версии:
- **P0**: analyze, write, clarify
- **P1**: track, specify, plan
- **P2**: tasks, plot-check, timeline, relations, world-check
- **Другие**: constitution, expert, track-init

### В5: Можно ли откатить обновление после его установки?

**О**: Да. `novel upgrade` автоматически создает каталог `backup/`:
```bash
# Если после обновления возникли проблемы, восстановите из автоматической резервной копии
rm -rf .claude .specify
mv backup/.claude .claude
mv backup/.specify .specify
```

Если вы делали ручную резервную копию:
```bash
# Удалите файлы после обновления
rm -rf .claude

# Восстановите ручную резервную копию
mv .claude.backup .claude
```

### В6: Нужно ли обновлять npm пакеты?

**О**: Да, сначала обновите глобальный пакет:
```bash
npm install -g novel-writer-cn@latest
```
Затем выполните обновление проекта:
```bash
novel upgrade
```

### В7: Влияет ли пространство имен на использование команд?

**О**: Нет. Пространство имен полностью прозрачно для пользователя:
- ✅ Вы по-прежнему используете команды `/specify`, `/write`, `/track` и т. д.
- ✅ Инструменты AI автоматически найдут файлы `novel.specify.md`, `novel.write.md` и т. д.
- ✅ Только имена файлов имеют префикс, способ использования остается неизменным.
- ✅ Преимущество: предотвращает конфликты с командами spec-kit или других инструментов.

### В8: Обязательно ли использование функции управления несколькими сюжетными линиями?

**О**: Нет, не обязательно. Управление несколькими сюжетными линиями в v0.12.0 является **необязательной дополнительной функцией**:
- Если структура вашей истории проста, вам не нужно добавлять главу 5.
- Если вам нужно управлять сложными многосюжетными историями, добавление этой функции позволит лучше отслеживать и проверять их.

### В9: Команды выдают ошибки после обновления?

**О**: Проверьте полноту обновления:
```bash
# 1. Убедитесь, что файлы команд обновлены (должен быть префикс novel.*)
ls .claude/commands/novel.*

# 2. Убедитесь, что скрипты существуют
ls .specify/scripts/bash/

# 3. Если обновление неполное, повторите его
novel upgrade --ai claude
```

### В10: Как использовать интерактивное обновление v0.16.5?

**О**: Интерактивное обновление позволяет вам свободно выбирать, какие компоненты обновлять:

```bash
novel upgrade -i
```

Появится следующий интерфейс:
```
? Выберите, что обновлять: (Нажмите <пробел> для выбора, <a> для выбора всех, <i> для инвертирования)
❯◉ Файлы команд (Commands)
 ◉ Файлы скриптов (Scripts)
 ◯ Файлы шаблонов (Templates)
 ◯ Файлы памяти (Memory)
```

Как использовать:
- **Клавиша пробела** - Выбрать/отменить выбор текущего элемента
- **Клавиша a** - Выбрать все/отменить выбор всех
- **Клавиша i** - Инвертировать выбор
- **Клавиша Enter** - Подтвердить выбор

**Рекомендуемые сценарии использования**:
1. Первое обновление до v0.16.5 (можно выбрать обновление отдельных компонентов)
2. Когда вы не уверены, какие компоненты нужно обновить
3. Когда вы хотите более детально контролировать процесс обновления

### В11: Можно ли обновить только файлы команд, не обновляя скрипты?

**О**: Да! v0.16.5 поддерживает выборочное обновление:

```bash
# Обновить только файлы команд
novel upgrade --commands

# Обновить только файлы скриптов
novel upgrade --scripts

# Обновить только файлы шаблонов
novel upgrade --templates

# Обновить только файлы памяти
novel upgrade --memory

# Комбинированное использование (команды + скрипты)
novel upgrade --commands --scripts
```

**Сценарии использования**:

1. **Обновление только команд** - получить новейшие функции команд, сохранив стабильность скриптов:
   ```bash
   novel upgrade --commands
   ```

2. **Обновление только скриптов** - исправить ошибки в скриптах, не затрагивая команды:
   ```bash
   novel upgrade --scripts
   ```

3. **Обновление фреймворка** - обновить файлы шаблонов и памяти:
   ```bash
   novel upgrade --templates --memory
   ```

**Примечание**:
- Поведение по умолчанию (без указания опций) обновляет **команды и скрипты**.
- Файлы шаблонов и памяти по умолчанию **не обновляются** (если явно не указано иное).
- Можно комбинировать с опцией `--ai`:
  ```bash
  novel upgrade --ai claude --commands
  ```

### В12: Что такое «Решение по контролю температуры AI» в v0.16.5?

**О**: Это решение проблем с написанием, вызванных низкими параметрами температуры в инструментах программирования AI (Claude Code, Cursor).

**Контекст проблемы**:
- Инструменты AI используют низкую температуру (0.0-0.2) для обеспечения точности кода.
- Низкая температура приводит к **механистичности, плоскости и отсутствию эмоций** при создании романов.
- Пользователи не могут напрямую регулировать параметр температуры.

**Решение**:
В команду `write.md` добавлены **инструкции по усилению творчества**, которые с помощью явного промпта компенсируют ограничения низкой температуры:

```bash
# Получите инструкции по усилению творчества после обновления
novel upgrade --commands
```

**Инструкции по усилению творчества включают**:
1. **Требования к эмоциональному разнообразию и выразительности** - избегать шаблонных описаний.
2. **Стандарты естественности диалогов** - отказаться от пояснительных диалогов, использовать реальный ритм.
3. **Оживление описаний сцен** - использовать сенсорные детали, чтобы «оживить» сцену.
4. **Усиление конфликтов и напряжения** - создавать драматическое напряжение, избегать прямолинейности.
5. **Требования к изменению темпа** - чередовать длинные и короткие предложения, менять темп.

**Результат**:
- Компенсация эффекта высокой температуры на 70-80%.
- Более богатые эмоции персонажей, более естественные диалоги.
- Более живые описания сцен, большее напряжение в конфликтах.

**Подробная документация**:
```bash
# Расположение документации
docs/temperature-guide.md
```

**Проверка обновления**:
```bash
# Проверьте, содержит ли write.md инструкции по усилению творчества
grep "创作强化指令" .claude/commands/novel.write.md
```

### В13: Почему инструкции по усилению творчества в v0.16.5 приводят к 95% AI-концентрации?

**О**: В v0.16.5 «инструкции по усилению творчества» столкнулись с серьезной проблемой при практическом использовании.

**Обнаружение проблемы**:
- Отзывы пользователей: при использовании v0.16.5 сгенерированный контент показывает **95% AI-концентрации** по данным Tencent Zhuque.
- Сравнительные данные: на той же платформе рукописный контент может достигать **0% AI-концентрации**.
- Корень проблемы: **неверное направление** инструкций v0.16.5 по «компенсации низкой температуры».

**Анализ ошибочных инструкций** (v0.16.5):
```markdown
❌ Пример ошибки 1: «Каждая сцена должна содержать сенсорные детали — как минимум 3 сенсорных ощущения»
   → Приводит к: принудительному нагромождению, что увеличивает признаки AI.

❌ Пример ошибки 2: «Комнату наполнял запах плесени, единственный источник света — сероватый лунный свет, пробивающийся сквозь щели в шторах...»
   → Проблема: использование большого количества высокочастотных AI-слов (например, «наполнял», «единственный», «до тех пор»).

❌ Пример ошибки 3: «Не успев договорить, она уже повернулась и ушла. Он бросился, чтобы схватить ее за руку, она резко отдернула свою...»
   → Проблема: чрезмерная драматизация, типичный AI-стиль.
```

**Почему это не сработало**:
1. **Неверная концепция**: предполагалось, что «компенсация» требует «усиления» и «обогащения».
2. **Противоположное направление**: на самом деле AI-детекторы распознают AI именно по этим «обогащенным» признакам.
3. **Обратный эффект**: чем «живее» текст, тем больше он похож на AI, чем «лаконичнее» — тем больше на человека.

**Фактические данные**:
| Версия | Тип инструкций | AI-концентрация | Проверка Zhuque |
|---|---|---|---|
| v0.16.5 | Усиление творчества (нагромождение сенсорных деталей) | 95% | ❌ Не пройдено |
| Рукописный эталон | Лаконичность и сдержанность (белый лист) | 0% | ✅ Пройдено |

**Вывод**:
Провал v0.16.5 доказал: **компенсация низкой температуры должна стремиться не к «богатству», а к «естественности»**.

### В14: Как v0.17.0 решает проблему AI-детекции?

**О**: v0.17.0, основываясь на практических результатах достижения 0% AI-концентрации, полностью переработал стратегию противодействия AI-детекции.

**Основные улучшения**:

**1. Нормализация структуры абзацев (самое важное) ⭐**
```markdown
✅ Принцип: 30-50% абзацев состоят из одного предложения.
✅ Контроль длины абзаца: 50-100 слов.
✅ Выделение ключевой информации в отдельные абзацы.

Пример (0% AI-концентрации):
После Восстания Юнцзя северные равнины были оккупированы иноземцами.

Большинство знати и простолюдинов из центральных равнин мигрировали на юг, через реку.

Годы спустя Ван Цяо собрал сотню беженцев, чтобы они обрабатывали его землю.
```

**2. Черный список высокочастотных AI-слов**
```markdown
Запрещенные слова:
- Описательные: наполнял, единственный, шаткий, словно
- Действия: до тех пор, пока, внезапно, не успев договорить, невольно
- Психологические: внутренне думал, внезапно, внутри возникло
- Состояния: воздух застыл, время будто остановилось, нахмурился

Стратегия замены:
| ❌ AI-слово | ✅ Естественная замена |
| Наполнял запахом плесени | Был запах плесени / Сильный запах плесени |
| Единственный источник света | Только немного света / Всего лишь свет |
| Шаткий деревянный стол | Старый деревянный стол / Потрепанный деревянный стол |
| Он внутренне думал | Он думал / **Удалить** |
```

**3. Список запрещенных действий**
- ❌ Запрещено бессмысленное нагромождение (не пытайтесь насильно «набрать 3 вида чувств»).
- ❌ Запрещены цветистые метафоры («воздух застыл» → «тишина»).
- ❌ Запрещена чрезмерная драматизация (конфликты описывать кратко).
- ❌ Запрещены пояснительные диалоги (использовать реальный ритм диалога).
- ❌ Запрещены прямые описания психологии (подразумевать через действия).

**4. Принцип естественного письма**
- ✅ Метод исторического белого листа (для исторических фонов): излагать факты без украшений.
- ✅ Обработка разговорной речи (диалоги): паузы, ошибки, повторы.
- ✅ Ритм коротких предложений (повествование): 15-25 слов в предложении.
- ✅ Сдержанное описание (сцены): достаточно 1-2 деталей.

**Метод обновления**:
```bash
# 1. Обновите глобальный пакет
npm install -g novel-writer-cn@latest

# 2. Обновите команды проекта (получите новый write.md)
cd my-novel
novel upgrade --commands
```
# 3. Проверка новых спецификаций

grep "反AI检测写作规范" .claude/commands/novel.write.md
```

**Ожидаемые результаты**:
| Показатель | v0.16.5 | Цель v0.17.0 |
|------|---------|-------------|
| AI-концентрация | 95% | < 5% |
| Проверка Zhuque | ❌ Не пройдено | ✅ Пройдено |
| Основано на фактических данных | Теоретические предположения | 0% реальных кейсов |

**Проверка успешного обновления**:
```bash
# Проверить наличие новых спецификаций для обнаружения ИИ
grep "30-50% предложений в одном абзаце" .claude/commands/novel.write.md

# Проверить наличие черного списка высокочастотных слов ИИ
grep "пронизан" .claude/commands/novel.write.md

# Проверить удаление ошибочной инструкции "минимум 3 чувств"
! grep "минимум 3 чувственных переживаний" .claude/commands/novel.write.md && echo "✅ Ошибочная инструкция удалена"
```

**Просмотр полных спецификаций**:
```bash
# Подробная документация по обнаружению ИИ
cat spec/presets/anti-ai-detection.md
```

**Резюме**:
v0.17.0 переходит от «компенсирующего обогащения» к «точному ограничению», основанному на реальных успешных кейсах (0% AI-концентрации) для достижения эффективного обнаружения ИИ.

---

## Чек-лист обновления

### Перед обновлением
- [ ] Обновите пакет npm: `npm install -g novel-writer-cn@latest`
- [ ] Подтвердите версию: `novel -v` (должна быть 0.16.5+)
- [ ] Перейдите в каталог проекта: `cd my-novel`
- [ ] (Необязательно) Ручное резервное копирование: `cp -r .claude .claude.backup`

### Во время обновления

**Выбор метода**:
- [ ] **Рекомендуется**: Запустите `novel upgrade` (автоматическое резервное копирование, обновление всех платформ)
- [ ] **Интерактивный**: Запустите `novel upgrade -i` (интерактивный выбор обновлений)
- [ ] **Выборочный**: Запустите `novel upgrade --commands --scripts` (указание обновляемого контента)
- [ ] **Указание платформы**: Запустите `novel upgrade --ai claude` (обновление только конкретной платформы)
- [ ] Или выберите другой метод (метод 2/3)

**Новые опции в v0.16.5**:
- [ ] Используйте `-i` для интерактивного выбора
- [ ] Используйте `--commands` для обновления только файлов команд
- [ ] Используйте `--scripts` для обновления только файлов скриптов
- [ ] Используйте `--templates` для обновления файлов шаблонов (необязательно)
- [ ] Используйте `--memory` для обновления файлов памяти (необязательно)
- [ ] Используйте `--dry-run` для предварительного просмотра содержимого обновления
- [ ] Используйте `-y` для пропуска подтверждающих запросов

### После обновления

**Базовая проверка**:
- [ ] Проверьте количество команд: `ls .claude/commands/ | wc -l` (должно быть 14)
- [ ] Проверьте пространство имен: `ls .claude/commands/novel.*` (должно быть с префиксом novel.*)
- [ ] Проверьте расширенные функции: `grep "argument-hint" .claude/commands/novel.*.md`
- [ ] Проверьте целостность пользовательских данных: проверьте каталоги `stories/` и `spec/`

**Проверка функций v0.16.5**:
- [ ] Просмотрите отчет об обновлении (отображается автоматически)
- [ ] Подтвердите обновленные платформы: список AI-платформ в отчете
- [ ] Подтвердите количество обновленных файлов: статистика команд, скриптов, шаблонов, памяти
- [ ] Проверьте расположение резервных копий: `ls backup/` (должен быть каталог с временной меткой)
- [ ] Проверьте инструкции по усилению творчества: `grep "创作强化指令" .claude/commands/novel.write.md`

**Тестирование функций**:
- [ ] Протестируйте ключевые команды: запустите `/specify` или `/write` в AI-помощнике
- [ ] Протестируйте управление температурой ИИ: запустите `/write`, чтобы увидеть подсказки по усилению творчества
- [ ] Протестируйте несколько платформ (например, установите несколько): переключайтесь между различными AI-инструментами для тестирования команд

**Пример отчета об обновлении**:
```
✅ Обновление завершено!

Статистика обновления:
  • Версия: 0.16.4 → 0.16.5
  • AI-платформы: Claude Code, Cursor, Gemini CLI
  • Файлы команд: 42
  • Файлы скриптов: 16

📦 Местоположение резервной копии: backup/2025-10-12T15-30-45
```

### Устранение неполадок
- [ ] Если обновление не удалось, проверьте сообщения об ошибках
- [ ] Если команды неполные, повторно запустите `novel upgrade`
- [ ] Для отката из каталога `backup/` восстановите данные
- [ ] См. FAQ Q9-Q12 для решения распространенных проблем

---

## Получение помощи

- **GitHub Issues**: https://github.com/wordflowlab/novel-writer/issues
- **Документация**: https://github.com/wordflowlab/novel-writer#readme
- **CHANGELOG**: Просмотрите CHANGELOG.md для получения информации об изменениях в версиях

---

## История версий

### v0.16.5 (2025-10-12)
- 🎯 Улучшения команды upgrade (интерактивный выбор, выборочное обновление)
- 🌍 Гибкое обновление для всех 13 AI-платформ
- 💾 Выборочное резервное копирование (резервное копирование только обновляемого контента)
- 📊 Подробные отчеты и статистика обновлений
- 🎨 Решение для управления температурой ИИ (инструкции по усилению творчества)

### v0.15.0 (2025-10-11)
- 🏗️ Новая система сборки (на базе архитектуры spec-kit)
- 🔖 Поддержка пространств имен (Claude: префикс novel.*, Gemini: подкаталог novel/)
- 📦 Единое управление источниками (templates/commands/)
- 🚀 Упрощенный CLI (использование артефактов сборки, сокращение ~150 строк кода)

### v0.12.2 (2025-10-04)
- ✨ Добавлен слой улучшения Claude Code (все 14 команд)
- 📚 Создано руководство по обновлению

### v0.12.0 (2025-09-30)
- ✨ Добавлена многопоточная система управления
- 📝 Глава 5 specification.md

### v0.11.0 (2025-09-28)
- ✨ Добавлено практическое руководство по методологии SDD