# Описание каталога Spec

> Центр спецификаций и организации данных для Novel Writer

## 📁 Структура каталога

```
spec/
├── config.json                 # Основной конфигурационный файл
├── README.md                   # Данный файл
├── presets/                    # Пресеты методов письма
│   ├── anti-ai-detection.md    # Спецификация защиты от ИИ-детекторов
│   ├── golden-opening.md       # Правила "золотого начала"
│   ├── three-act/              # Трехактная структура
│   ├── hero-journey/           # Путешествие героя
│   └── ...                     # Другие методы письма
├── checklists/                 # Чек-листы качества
│   ├── specification-quality.md
│   ├── plot-logic.md
│   └── ...
├── knowledge/                  # База знаний (накопленная информация)
│   ├── world/                  # Знания о мире
│   ├── rules/                  # Знания о правилах
│   ├── characters/             # Профили персонажей
│   └── research/               # Исследовательские материалы
└── tracking/                   # Данные отслеживания (данные во время выполнения)
    ├── plot-tracker.json       # Отслеживание сюжета
    ├── character-state.json    # Состояние персонажей
    ├── relationships.json      # Сеть взаимоотношений
    └── timeline.json           # Временная шкала
```

---

## 🎯 Обязанности каталогов

### Уровень спецификаций (неизменяемое содержимое)

**`presets/`** - Пресеты методов письма
- Обязанность: Хранение шаблонов и спецификаций различных методов письма.
- Особенность: Эти файлы являются «только для чтения» и не должны изменяться.
- Пример: `anti-ai-detection.md` (Спецификация защиты от ИИ-детекторов), `golden-opening.md` (Правила "золотого начала").
- Стратегия обновления: Может быть безопасно перезаписано командой `novel upgrade`.

**`checklists/`** - Чек-листы качества
- Обязанность: Хранение стандартов проверки качества.
- Особенность: Стандартизированные пункты проверки, которые AI читает при выполнении команды `/checklist`.
- Пример: `specification-quality.md` (Проверка полноты спецификаций), `plot-logic.md` (Проверка логики сюжета).
- Стратегия обновления: Может быть безопасно перезаписано командой `novel upgrade`.

**`config.json`** - Основной конфигурационный файл
- Обязанность: Определение конфигурации на уровне проекта.
- Особенность: Пользователь может изменять, но есть значения по умолчанию.
- Пример: `{"method": "three-act", "version": "0.5.2"}`
- Стратегия обновления: Слияние при обновлении (сохранение пользовательских настроек).

### Уровень знаний (накопленное содержимое)

**`knowledge/`** - База знаний
- Обязанность: Хранение знаний и материалов, накопленных в процессе создания.
- Особенность: Полностью создается и управляется пользователем, система не перезаписывает.
- Описание подкаталогов:
  - `world/` - Настройки мира (география, история, культура).
  - `rules/` - Знания о правилах (системы сил, законы).
  - `characters/` - Профили персонажей (глубокие настройки персонажей).
  - `research/` - Исследовательские материалы (ссылки, источники вдохновения).
- Стратегия обновления: **Никогда не перезаписывается**, пользовательский контент сохраняется полностью.

### Уровень отслеживания (данные во время выполнения)

**`tracking/`** - Данные отслеживания
- Обязанность: Хранение динамических данных отслеживания в процессе создания.
- Особенность: Постоянно обновляется по мере прогресса создания.
- Пример:
  - `plot-tracker.json` - Запись прогресса сюжета.
  - `character-state.json` - Запись текущего состояния персонажей.
  - `relationships.json` - Запись сети взаимоотношений персонажей.
  - `timeline.json` - Запись временной шкалы событий.
- Стратегия обновления: **Никогда не перезаписывается**, пользовательские данные сохраняются полностью.

---

## 🔍 Протокол запросов (рекомендуется)

При выполнении различных команд AI должен запрашивать соответствующие файлы в следующем порядке, обеспечивая полноту контекста и правильность приоритетов.

### Этап подготовки к созданию

**Применимые команды**: `/constitution`, `/specify`, `/clarify`

**Порядок запроса**:
1. **Сначала запросить**: `memory/novel-constitution.md` (Конституция создания - высший принцип).
2. **Затем запросить**: `memory/style-reference.md` (Справочник по стилю - если существует).
3. **Последним запросить**: `spec/presets/` (Пресеты методов письма).

**Цель**: Обеспечить соблюдение принципов создания и указаний по стилю при определении спецификаций истории.

---

### Этап планирования

**Применимые команды**: `/plan`, `/tasks`

**Порядок запроса**:
1. **Сначала запросить**: `memory/novel-constitution.md` (Принципы создания).
2. **Затем запросить**: `stories/*/specification.md` (Спецификации истории).
3. **Затем запросить**: `spec/presets/golden-opening.md` (Если это предварительное планирование).
4. **Последним запросить**: `spec/knowledge/` (База знаний).

**Цель**: Создание плана создания, соответствующего спецификациям и принципам.

---

### Этап конкретного написания

**Применимые команды**: `/write`

**Порядок запроса (Важно!)**:
1. **Сначала запросить**: `memory/novel-constitution.md` (Конституция создания).
2. **Затем запросить**: `memory/style-reference.md` (Справочник по стилю - если сгенерирован через `/book-internalize`).
3. **Затем запросить**: `stories/*/specification.md` (Спецификации истории).
4. **Затем запросить**: `stories/*/creative-plan.md` (План создания).
5. **Затем запросить**: `stories/*/tasks.md` (Текущие задачи).
6. **Затем запросить**: Файлы из `spec/tracking/`:
   - `character-state.json` (Состояние персонажей).
   - `relationships.json` (Сеть взаимоотношений).
   - `plot-tracker.json` (Отслеживание сюжета).
7. **Затем запросить**: Соответствующие файлы из `spec/knowledge/` (Мир, профили персонажей).
8. **Затем запросить**: `spec/presets/anti-ai-detection.md` (Спецификация защиты от ИИ-детекторов).
9. **Условный запрос**: Если это первые три главы, дополнительно запросить `spec/presets/golden-opening.md`.

**Цель**: Обеспечить наличие полного контекста при написании, соответствующего всем спецификациям и существующим настройкам.

---

### Этап проверки качества

**Применимые команды**: `/analyze`, `/checklist`, `/track`

**Порядок запроса**:
1. **Сначала запросить**: `memory/novel-constitution.md` (Для проверки соответствия конституции).
2. **Затем запросить**: `stories/*/specification.md` (Для проверки полноты выполнения спецификаций).
3. **Затем запросить**: `stories/*/creative-plan.md` (Для проверки выполнения плана).
4. **Затем запросить**: Все файлы из `spec/tracking/` (Для проверки согласованности).
5. **Затем запросить**: `spec/checklists/` (Использование стандартизированных чек-листов).
6. **Условный запрос**: Если это первые три главы, использовать чек-лист из `spec/presets/golden-opening.md`.

**Цель**: Всесторонняя проверка качества контента, выявление проблем и предоставление рекомендаций по улучшению.

---

## ⚙️ Приоритет правил

При конфликте правил из разных файлов обрабатываются в следующем порядке:

### Порядок приоритета (от высшего к низшему)

1. **Непосредственные команды пользователя** (высший приоритет)
   - Конкретные требования пользователя в команде.
   - Пример: «В этой главе не используй правила золотого начала, я хочу медленно создавать атмосферу».
   - Эффект: Переопределяет все предустановленные правила.

2. **Конституция создания** (`memory/novel-constitution.md`)
   - Высшие принципы создания проекта.
   - Пример: «В этом произведении запрещено изображать сцены насилия».
   - Эффект: Переопределяет все предустановленные спецификации.

3. **Справочник по стилю** (`memory/style-reference.md`)
   - Руководство по стилю для сравниваемых произведений.
   - Пример: «Используй короткие предложения, избегай витиеватых метафор».
   - Эффект: Влияет на конкретный стиль письма.

4. **Спецификации истории** (`stories/*/specification.md`)
   - Конкретные требования текущей истории.
   - Пример: «Целевая аудитория — мужчины 15-25 лет».
   - Эффект: Влияет на позиционирование и стиль контента.

5. **Пресеты методов письма** (`spec/presets/`)
   - Стандартизированные спецификации письма.
   - Пример: `anti-ai-detection.md`, `golden-opening.md`.
   - Эффект: Предоставляет базовые спецификации и лучшие практики.

6. **База знаний и данные отслеживания** (`spec/knowledge/`, `spec/tracking/`)
   - Существующие настройки и состояния.
   - Пример: Персонаж мертв, больше не может появляться.
   - Эффект: Обеспечивает согласованность.

### Пример разрешения конфликтов

**Сценарий 1**: Золотое начало против Конституции создания
- **Конфликт**: `golden-opening.md` требует прямого конфликта в первой главе, но `constitution.md` требует «медленного начала с акцентом на создание атмосферы».
- **Решение**: Следовать `constitution.md`, изменить стратегию начала.
- **Основание**: Приоритет Конституции создания выше предустановленных правил.

**Сценарий 2**: Справочник по стилю против Защиты от ИИ
- **Конфликт**: `style-reference.md` говорит, что сравниваемое произведение использует витиеватые метафоры, но `anti-ai-detection.md` говорит избегать витиеватых метафор.
- **Решение**: Отдать приоритет `style-reference.md`, но умеренно сдерживать витиеватые метафоры.
- **Основание**: Приоритет Справочника по стилю немного выше, но требуется баланс.

**Сценарий 3**: Непосредственная команда пользователя против всех пресетов
- **Конфликт**: Пользователь говорит «В этой главе используй много описаний окружения для создания атмосферы», но все спецификации говорят ограничивать описания.
- **Решение**: Полностью следовать команде пользователя.
- **Основание**: Непосредственные команды пользователя имеют высший приоритет.

---

## 🚀 Лучшие практики

### 1. При инициализации проекта

Рекомендуемый порядок создания:
1. Выполнить `/constitution` для создания `memory/novel-constitution.md`.
2. (Опционально) Выполнить `/book-analyze` + `/book-internalize` для генерации `memory/style-reference.md`.
3. Выполнить `/specify` для создания `stories/*/specification.md`.
4. Выполнить `/plan` для создания `stories/*/creative-plan.md`.
5. Перед началом создания вручную создать соответствующие файлы в `spec/knowledge/` (мир, персонажи и т. д.).

### 2. В процессе создания

**Перед каждым написанием**:
- Убедиться, что данные в `spec/tracking/` актуальны.
- Проверить, есть ли новые персонажи или настройки, которые нужно записать в `spec/knowledge/`.

**Каждые 5 глав**:
- Выполнить `/analyze` для проверки качества.
- Выполнить `/track` для обновления данных отслеживания.
- При необходимости выполнить `/checklist` для специализированной проверки.

### 3. При обновлении версии

**Безопасное обновление**:
```bash
novel upgrade
```

**Стратегия обновления**:
- `spec/presets/` - Будет обновлено (добавлены лучшие спецификации).
- `spec/checklists/` - Будет обновлено (добавлены пункты проверки).
- `spec/config.json` - Будет обновлено путем слияния (ваши пользовательские настройки сохранятся).
- `spec/knowledge/` - **Никогда не перезаписывается**.
- `spec/tracking/` - **Никогда не перезаписывается**.

### 4. Управление несколькими проектами

Если вы одновременно работаете над несколькими произведениями:
- У каждого произведения будет свой каталог `spec/`.
- Однако, пресеты из `presets/` можно использовать совместно (через символические ссылки или копирование).
- `knowledge/` и `tracking/` должны быть строго независимыми для каждого проекта.

---

## 📝 Правила именования файлов

### Каталог `knowledge/`

**Рекомендуемое именование**:
- Мир: `world/geography.md`, `world/history.md`
- Правила: `rules/power-system.md`, `rules/magic-rules.md`
- Персонажи: `characters/protagonist.md`, `characters/villain.md`
- Исследования: `research/[Название произведения]-analysis.md`

### Каталог `tracking/`

**Фиксированное именование** (генерируется системой, не изменять):
- `plot-tracker.json`
- `character-state.json`
- `relationships.json`
- `timeline.json`
- `validation-rules.json`

### Каталог `checklists/`

**Рекомендуемое именование**:
- Тип спецификации: `specification-*.md`
- Тип контента: `plot-*.md`, `character-*.md`, `world-*.md`
- Тип стиля: `style-*.md`, `dialogue-*.md`

---

## 🔧 Продвинутые приемы

### Прием 1: Использование символических ссылок для общих пресетов

Если вы хотите использовать одни и те же пресеты для нескольких проектов:

```bash
# В проекте A
cd project-a/spec
ln -s /path/to/shared-presets presets

# Проект B может сделать то же самое
cd project-b/spec
ln -s /path/to/shared-presets presets
```

### Прием 2: Создание личной библиотеки спецификаций

Создайте каталог `~/.novel-writer/presets/`, где будут храниться ваши личные, обобщенные правила письма, и затем ссылайтесь на них в проектах.

### Прием 3: Использование контроля версий Git

Настоятельно рекомендуется включить каталог `spec/` в систему контроля версий Git:

```bash
# .gitignore
spec/tracking/*.json  # Данные отслеживания не включать в контроль версий
spec/knowledge/      # Базу знаний можно включать выборочно

# А это следует включить:
spec/presets/
spec/checklists/
spec/config.json
```

---

## ❓ Часто задаваемые вопросы

### В1: В чем разница между `knowledge/` и `tracking/`?

**О**:
- `knowledge/` — это «статические знания», например, основные настройки персонажей, правила мира, которые не меняются часто.
- `tracking/` — это «динамическое состояние», например, текущее местоположение персонажа, его отношения, эмоции, которые могут меняться каждую главу.

### В2: Можно ли удалить ненужные методы из `presets/`?

**О**:
Можно, но не рекомендуется. Их сохранение не займет много места, и они могут пригодиться в будущем. Если вы действительно хотите удалить, не забудьте сделать резервную копию.

### В3: Что делать, если `presets/` был перезаписан после обновления?

**О**:
Это нормально. `presets/` спроектирован так, чтобы его можно было безопасно перезаписывать. Если вы внесли пользовательские изменения в какой-либо пресет, вам следует:
1. Скопировать его в каталог `memory/` или `knowledge/`.
2. Или переименовать (например, `anti-ai-detection-custom.md`).

### В4: Могу ли я создавать свои собственные пресеты?

**О**:
Конечно! Создавайте любые необходимые файлы `.md` в каталогах `presets/` или `memory/`, и AI обнаружит их при чтении каталога.

---

## 📚 Связанные документы

- **Руководство по процессу создания**:`docs/workflow.md`
- **Подробное описание команд**:`docs/commands.md`
- **Лучшие практики**:`docs/best-practices.md`
- **Руководство по обновлению**:`docs/upgrade-guide.md`

---

**Версия**: v1.0.0
**Дата обновления**: 2025-01-14
**Поддерживается**: Novel Writer Team