```markdown
# Руководство по локальной разработке

Это руководство описывает, как разрабатывать и тестировать Novel Writer локально, без необходимости публиковать версии или делать коммиты в основную ветку.

## 1. Клонирование и управление ветками

```bash
git clone https://github.com/wordflowlab/novel-writer.git
cd novel-writer
# Работайте в функциональной ветке
git checkout -b feature/your-feature
```

## 2. Настройка окружения

### 2.1 Окружение Python

```bash
# Создайте виртуальное окружение с помощью uv (рекомендуется)
uv venv
source .venv/bin/activate  # macOS/Linux
# Windows PowerShell: .venv\Scripts\Activate.ps1

# Установите зависимости
uv pip install -e .
```

### 2.2 Окружение Node.js (для цепочки инструментов)

```bash
# Установите зависимости Node.js
npm install

# Или используйте yarn
yarn install
```

## 3. Прямой запуск CLI (самая быстрая обратная связь)

Запускайте CLI напрямую без установки:

```bash
# Из корневого каталога репозитория
python -m src.novel_cli --help
python -m src.novel_cli style my-novel --genre fantasy --ai claude
```

Или через скриптовый файл:

```bash
python src/novel_cli/__init__.py story my-novel --plot adventure
```

## 4. Использование установки в режиме редактирования (изолированное окружение)

Создайте изолированное окружение, чтобы обеспечить согласованность разрешения зависимостей с окружением пользователя:

```bash
# Создайте и активируйте виртуальное окружение
uv venv
source .venv/bin/activate

# Установка в режиме редактирования
uv pip install -e .

# Теперь команда 'novel' доступна
novel --help
novel style my-book --genre scifi
```

После внесения изменений в код переустановка не требуется (режим редактирования).

## 5. Использование uvx для прямого запуска из Git

### 5.1 Запуск из локального пути

```bash
uvx --from . novel outline my-story --chapters 10
```

### 5.2 Запуск из определенной ветки (без слияния)

```bash
# Сначала отправьте рабочую ветку
git push origin feature/your-feature

# Запуск из ветки
uvx --from git+https://github.com/wordflowlab/novel-writer.git@feature/your-feature novel write chapter-1
```

### 5.3 Запуск по абсолютному пути (из любого места)

```bash
# Используйте абсолютный путь
uvx --from /Users/yourname/dev/novel-writer novel --help

# Установите переменную окружения для удобства
export NOVEL_SRC=/Users/yourname/dev/novel-writer
uvx --from "$NOVEL_SRC" novel style my-book

# Определите функцию оболочки (необязательно)
novel-dev() { uvx --from /Users/yourname/dev/novel-writer novel "$@"; }
novel-dev --help
```

## 6. Права доступа к скриптам

### POSIX системы (macOS/Linux)

```bash
# Проверьте права доступа скриптов к исполнению
ls -l scripts/*.sh
# Ожидается вывод -rwxr-xr-x

# При необходимости добавьте права на выполнение
chmod +x scripts/*.sh
```

### Windows системы

В Windows используются скрипты `.ps1`, `chmod` не требуется.

## 7. Запуск тестов

```bash
# Запустите все тесты
pytest

# Запустите конкретный тест
pytest tests/test_style.py

# Запустите с покрытием
pytest --cov=src --cov-report=html
```

## 8. Проверка качества кода

```bash
# Проверка кода Python
ruff check src/
black --check src/
mypy src/

# Проверка документации Markdown
markdownlint docs/*.md

# Проверка орфографии
cspell docs/*.md specs/**/*.md
```

## 9. Сборка локального пакета (необязательно)

Проверьте конфигурацию сборки пакета:

```bash
# Соберите wheel
uv build
ls dist/

# Протестируйте установку в новом окружении
cd /tmp
uv venv test-env
source test-env/bin/activate
pip install /path/to/novel-writer/dist/*.whl
novel --help
```

## 10. Использование временного рабочего пространства

Создайте временное пространство для тестирования команд инициализации:

```bash
# Создайте временный каталог
mkdir -p /tmp/novel-test && cd /tmp/novel-test

# Тестирование инициализации
novel style test-novel --genre romance --ai gemini

# Просмотрите сгенерированную структуру
tree -L 2
```

## 11. Отладка сетевых проблем

При возникновении проблем с сетью или TLS:

```bash
# Пропустите проверку TLS (только для локального тестирования)
novel style my-book --skip-tls

# Используйте прокси
export HTTP_PROXY=http://localhost:8080
export HTTPS_PROXY=http://localhost:8080
novel write --api-endpoint http://localhost:8000
```

## 12. Краткое изложение для быстрой итерации

| Операция | Команда |
|------|------|
| Прямой запуск CLI | `python -m src.novel_cli --help` |
| Установка в режиме редактирования | `uv pip install -e .`, затем `novel ...` |
| Локальный запуск uvx (корневой каталог репозитория) | `uvx --from . novel ...` |
| Локальный запуск uvx (абсолютный путь) | `uvx --from /path/to/novel-writer novel ...` |
| uvx из ветки Git | `uvx --from git+URL@branch novel ...` |
| Сборка wheel | `uv build` |
| Запуск тестов | `pytest` |
| Проверка кода | `ruff check src/` |

## 13. Очистка артефактов сборки

Быстро очистите сборку и виртуальное окружение:

```bash
# Очистка артефактов сборки Python
rm -rf .venv dist build *.egg-info

# Очистка зависимостей Node.js
rm -rf node_modules

# Очистка сгенерированной документации
rm -rf docs/_site

# Очистка отчетов о покрытии тестов
rm -rf htmlcov .coverage
```

## 14. Частые вопросы

| Вопрос | Решение |
|------|---------|
| `ModuleNotFoundError: typer` | Запустите `uv pip install -e .` |
| Скрипт не исполняется (Linux/Mac) | Запустите `chmod +x scripts/*.sh` |
| Шаг Git пропущен | Проверьте, был ли передан флаг `--no-git` или не установлен ли Git |
| Ошибка подключения к API | Проверьте конфигурацию API-ключа и сетевое подключение |
| Отображение китайских иероглифов некорректно | Убедитесь, что терминал использует кодировку UTF-8 |
| Сборка документации завершилась ошибкой | Убедитесь, что установлены DocFX и .NET SDK |

## 15. Рабочий процесс разработки

### 15.1 Процесс разработки функций

1. Создайте функциональную ветку
2. Напишите тесты (TDD)
3. Реализуйте функцию
4. Протестируйте локально
5. Проведите обзор кода
6. Отправьте PR

### 15.2 Процесс разработки документации

1. Отредактируйте файлы документации
2. Локальный предварительный просмотр: `docfx docs/docfx.json --serve`
3. Проверьте ссылки и форматирование
4. Отправьте изменения

### 15.3 Процесс выпуска

1. Обновите номер версии (`src/__version__.py`)
2. Обновите CHANGELOG.md
3. Создайте тег: `git tag v1.0.0`
4. Отправьте тег: `git push origin v1.0.0`
5. GitHub Actions автоматически выполнит выпуск

## 16. Интеграция с инструментами ИИ

### 16.1 Claude Code

```bash
# Создайте конфигурационный файл .claude в корне проекта
echo '{"ai_provider": "claude"}' > .claude/config.json
```

### 16.2 GitHub Copilot

```bash
# Настройте правила игнорирования Copilot
echo "specs/" >> .copilotignore
echo "*.generated.py" >> .copilotignore
```

### 16.3 Cursor

```bash
# Настройте параметры Cursor
echo '{"ai_model": "gpt-4"}' > .cursor/settings.json
```

## 17. Анализ производительности

```bash
# Анализ производительности CPU
python -m cProfile -o profile.stats src/novel_cli/__init__.py write chapter-1
python -m pstats profile.stats

# Анализ использования памяти
python -m memory_profiler src/novel_cli/__init__.py style my-book
```

## 18. Следующие шаги

- Прочтите [Руководство по вкладу](CONTRIBUTING.md) для ознакомления с правилами кодирования
- Ознакомьтесь с [Архитектурной документацией](architecture.md) для понимания дизайна системы
- Обратитесь к [Документации API](api.md) для изучения спецификаций интерфейса
- Присоединяйтесь к [Сообществу разработчиков](https://github.com/wordflowlab/novel-writer/discussions)

---

💡 **Совет**: Возникли проблемы в процессе разработки? Обратитесь к [FAQ](faq.md) или задайте вопрос в [Issues](https://github.com/wordflowlab/novel-writer/issues).
```