```markdown
# Плагин-центр Novel Writer PRD

## 1. Обзор продукта

### 1.1 Название продукта
**Система плагинов Novel Writer** - Система плагинов для инструментов создания романов

### 1.2 Позиционирование продукта
Легковесная система плагинов для Novel Writer, позволяющая пользователям добавлять функции по мере необходимости с помощью механизма модульного расширения, сохраняя при этом основной инструмент компактным и эффективным. Система плагинов глубоко интегрирована в существующую командную архитектуру, обеспечивая пользователям плагинов абсолютно такой же опыт использования, как и основным командам.

### 1.3 Философия дизайна
- **Простота прежде всего**: Основные функции должны быть простыми, а сложные функции предоставляются через плагины.
- **Опциональное улучшение**: Пользователи выбирают расширения функций по мере необходимости.
- **Бесшовная интеграция**: Команды плагинов автоматически вставляются в каталог команд AI-помощника.
- **Экспертная поддержка**: Плагины могут предоставлять экспертные режимы для глубокого руководства.
- **Использование лучшего**: Вдохновлено модульной идеей BMAD, но сохраняет легковесность.

### 1.4 Целевая аудитория
- **Продвинутые авторы**: Нуждающиеся в расширенных функциях, таких как перевод, анализ и т. д.
- **Профессиональные команды**: Нуждающиеся в настраиваемых рабочих процессах.
- **Разработчики плагинов**: Желающие расширить функциональность Novel Writer.

## 2. Архитектура системы

### 2.1 Диаграмма архитектуры системы плагинов

```
┌─────────────────────────────────────────────────┐
│              Ядро Novel Writer                    │
│                                                   │
│  ┌──────────────────────────────────────────┐   │
│  │         Менеджер плагинов                  │   │
│  │  ┌────────┬────────┬────────────────┐    │   │
│  │  │Реестр│ Загрузчик│ Внедряющий команды│    │   │
│  │  └────────┴────────┴────────────────┘    │   │
│  └──────────────────────────────────────────┘   │
│                                                   │
│  ┌──────────────────────────────────────────┐   │
│  │         Система команд                     │   │
│  │  ┌─────────────┬──────────────────┐      │   │
│  │  │ Основные команды│ Команды плагинов │      │   │
│  │  └─────────────┴──────────────────┘      │   │
│  └──────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────┐
│           Интеграция с AI-платформой             │
│  ┌──────┬──────┬──────┬──────┬──────────┐     │
│  │Claude│Cursor│Gemini│Windsurf│  ...     │     │
│  └──────┴──────┴──────┴──────┴──────────┘     │
└─────────────────────────────────────────────────┘
```

### 2.2 Жизненный цикл плагина

```mermaid
graph LR
    A[Обнаружение плагина] --> B[Установка плагина]
    B --> C[Регистрация команды]
    C --> D[Генерация файла команды AI]
    D --> E[Плагин готов]
    E --> F[Использование плагина]
    F --> G[Обновление/Удаление]
```

### 2.3 Структура файлов (реализация)

```bash
novel-project/
├── plugins/                         # Каталог плагинов
│   └── [plugin-name]/              # Конкретный плагин
│       ├── config.yaml             # Конфигурация плагина
│       ├── commands/               # Команды плагина
│       │   └── *.md
│       └── experts/                # Эксперты плагина (опционально)
│           └── *.md
├── experts/                         # Каталог экспертных режимов
│   ├── core/                       # Основные эксперты
│   │   ├── plot.md                # Эксперт по структуре сюжета
│   │   ├── character.md           # Эксперт по созданию персонажей
│   │   ├── world.md               # Эксперт по дизайну мира
│   │   └── style.md               # Эксперт по стилю и языку
│   └── plugins/                    # Эксперты плагинов
│       └── [plugin-name]/
│           └── *.md
├── templates/commands/              # Шаблоны команд
│   ├── *.md                       # Основные команды
│   └── expert.md                  # Команды экспертов
└── .claude/commands/               # Каталог команд AI
    ├── *.md                        # Основные команды
    ├── expert.md                   # Команды экспертов
    └── plugin-*.md                 # Команды плагинов
```

## 3. Требования к функциональности

### 3.1 CLI для управления плагинами

#### 3.1.1 Реализованные команды управления плагинами
```bash
novel plugins                        # Показать справку по плагинам
novel plugins list                   # Список установленных плагинов
novel plugins:list                   # То же (формат подкоманды)
novel plugins add <name>             # Установить плагин
novel plugins:add <name>             # То же (формат подкоманды)
novel plugins remove <name>          # Удалить плагин
novel plugins:remove <name>          # То же (формат подкоманды)
```

#### 3.1.2 Поддержка плагинов при инициализации
```bash
novel init my-novel --plugins translate       # Предварительно установить плагин перевода
novel init my-novel --with-experts           # Включить экспертные режимы
novel init my-novel --with-experts --plugins translate  # Оба варианта
```

### 3.2 Реестр плагинов

#### 3.2.1 Структура реестра
```json
{
  "version": "1.0.0",
  "plugins": {
    "translate": {
      "name": "translate",
      "displayName": "Плагин для перевода и локализации",
      "description": "Перевод китайских романов на английский и локализация",
      "version": "1.0.0",
      "author": "Novel Writer Team",
      "repository": "https://github.com/novelwriter/plugin-translate",
      "commands": [
        "translate",
        "translate-verify",
        "translate-glossary",
        "translate-batch"
      ],
      "dependencies": {
        "novel-writer": ">=0.5.0"
      }
    },
    "analyzer": {
      "name": "analyzer",
      "displayName": "Плагин для анализа произведений",
      "description": "Анализ и разбор выдающихся произведений",
      "version": "1.0.0",
      "commands": [
        "analyze",
        "analyze-structure",
        "analyze-style"
      ]
    }
  }
}
```

#### 3.2.2 Фактическая конфигурация плагина (config.yaml)
```yaml
name: novel-translate
version: 1.0.0
description: Плагин для перевода и локализации китайских и английских романов
author: Novel Writer Team
type: feature
license: MIT

# Команды, предоставляемые плагином
commands:
  - id: translate
    file: commands/translate.md
    description: Выполнить профессиональный процесс перевода с китайского на английский
  - id: polish
    file: commands/polish.md
    description: Улучшить английское выражение и стилистическую шлифовку

# Экспертные режимы, предоставляемые плагином
experts:
  - id: translate
    file: experts/translate.md
    title: Эксперт по переводу и локализации
    description: Предоставить глубокие стратегии перевода и рекомендации по культурной адаптации

# Настройки плагина
settings:
  defaultPlatform: general
  platforms:
    - general
    - reddit
    - medium
    - wattpad

# Требования к зависимостям
dependencies:
  core: ">=0.5.0"
```

### 3.3 Система внедрения команд

#### 3.3.1 Процесс внедрения команд
1. **Чтение шаблонов команд плагина**: Из `.specify/plugins/[name]/commands/`
2. **Копирование в каталог шаблонов**: Ссылка на `templates/commands/plugins/[name]/`
3. **Запуск повторной генерации**: Выполнение внутренней логики генерации команд
4. **Обновление каталога команд AI**: Генерация в `.claude/commands/`, `.cursor/commands/` и т. д.

#### 3.3.2 Соглашение об именовании команд
- Основные команды: `style.md`, `story.md` и т. д.
- Команды плагинов: `[plugin]-[command].md`
  - Например: `translate-verify.md`, `analyzer-structure.md`

#### 3.3.3 Механизм автоматического обновления
```typescript
class CommandInjector {
  async injectPluginCommands(pluginName: string): Promise<void> {
    const plugin = await this.loadPlugin(pluginName);

    // 1. Копирование шаблонов команд
    for (const command of plugin.commands) {
      const source = path.join(plugin.path, 'commands', command.template);
      const target = path.join(projectPath, 'templates/commands/plugins', pluginName, command.name + '.md');
      await fs.copy(source, target);
    }

    // 2. Повторная генерация файлов команд AI
    await this.regenerateAICommands();
  }

  async regenerateAICommands(): Promise<void> {
    // Чтение всех шаблонов команд (основные + плагины)
    const coreCommands = await this.loadCoreCommands();
    const pluginCommands = await this.loadPluginCommands();
    const allCommands = [...coreCommands, ...pluginCommands];

    // Генерация файлов команд для каждой AI-платформы
    for (const platform of ['claude', 'cursor', 'gemini', 'windsurf']) {
      await this.generateCommandsForPlatform(platform, allCommands);
    }
  }
}
```

### 3.4 Реализация плагина

#### 3.4.1 Менеджер плагинов (src/plugins/manager.ts)
```typescript
export class PluginManager {
  private pluginsDir: string
  private commandsDir: string
  private expertsDir: string

  constructor(projectRoot: string) {
    this.pluginsDir = path.join(projectRoot, 'plugins')
    this.commandsDir = path.join(projectRoot, '.claude', 'commands')
    this.expertsDir = path.join(projectRoot, 'experts')
  }

  async loadPlugins(): Promise<void>
  async listPlugins(): Promise<PluginConfig[]>
  async installPlugin(name: string, source?: string): Promise<void>
  async removePlugin(name: string): Promise<void>

  private async injectCommands(plugin: string, commands: any[]): Promise<void>
  private async registerExperts(plugin: string, experts: any[]): Promise<void>
}

interface PluginConfig {
  name: string
  version: string
  description: string
  type: 'feature' | 'expert' | 'workflow'
  commands?: Array<{
    id: string
    file: string
    description: string
  }>
  experts?: Array<{
    id: string
    file: string
    title: string
    description: string
  }>
  dependencies?: {
    core: string
  }
}
```

#### 3.4.2 Шаблон разработки плагина
```bash
novel plugin create my-plugin        # Создать шаблон плагина
```

Сгенерированная структура шаблона:
```
my-plugin/
├── package.json
├── index.js                        # Точка входа плагина
├── commands/                       # Шаблоны команд
│   └── my-command.md
├── scripts/                        # Скрипты выполнения
│   ├── bash/
│   └── powershell/
├── lib/                           # Логика плагина
└── test/                          # Файлы тестов
```

### 3.5 Интеграция экспертного режима

#### 3.5.1 Основные эксперты
Система плагинов глубоко интегрирована с экспертными режимами:
- **plot** - Эксперт по структуре сюжета
- **character** - Эксперт по созданию персонажей
- **world** - Эксперт по дизайну мира
- **style** - Эксперт по стилю и языку

#### 3.5.2 Эксперты плагинов
Плагины могут предоставлять своих собственных экспертов:
- Плагин **translate** → Эксперт по переводу и локализации
- Будущие плагины могут добавлять больше специализированных экспертов

#### 3.5.3 Использование экспертного режима
```bash
/expert              # Список всех доступных экспертов (основных + плагинов)
/expert plot         # Активировать эксперта по структуре сюжета
/expert translate    # Активировать эксперта по переводу (из плагина)
```

## 4. Техническая реализация

### 4.1 Загрузчик плагинов

```typescript
class PluginLoader {
  private plugins: Map<string, NovelWriterPlugin> = new Map();

  async loadPlugin(name: string): Promise<NovelWriterPlugin> {
    const pluginPath = path.join(PLUGINS_DIR, name);
    const packageJson = await fs.readJson(path.join(pluginPath, 'package.json'));

    // Проверка плагина
    this.validatePlugin(packageJson);

    // Загрузка плагина
    const plugin = require(pluginPath);

    // Инициализация
```
```
    if (plugin.onInstall) {
      await plugin.onInstall();
    }

    this.plugins.set(name, plugin);
    return plugin;
  }

  private validatePlugin(packageJson: any): void {
    // Проверка обязательных полей
    if (!packageJson.novelWriter) {
      throw new Error('Недопустимый плагин Novel Writer');
    }

    // Проверка совместимости версий
    const requiredVersion = packageJson.novelWriter.dependencies?.['novel-writer'];
    if (!this.checkVersionCompatibility(requiredVersion)) {
      throw new Error(`Плагин требует Novel Writer ${requiredVersion}`);
    }
  }
}
```

### 4.2 Обработка конфликтов команд

```typescript
class CommandRegistry {
  private commands: Map<string, CommandSource> = new Map();

  registerCommand(name: string, source: CommandSource): void {
    if (this.commands.has(name)) {
      // Обработка конфликта команд
      const existing = this.commands.get(name);
      if (existing.type === 'core') {
        throw new Error(`Невозможно переопределить основную команду: ${name}`);
      }

      // Конфликт команд плагина, используем пространство имен
      name = `${source.plugin}-${name}`;
    }

    this.commands.set(name, source);
  }
}
```

### 4.3 Управление конфигурацией

```typescript
class PluginConfigManager {
  async getConfig(pluginName: string): Promise<any> {
    const configPath = path.join(PLUGINS_DIR, pluginName, 'config.json');
    const defaultConfig = await this.getDefaultConfig(pluginName);
    const userConfig = await fs.readJson(configPath).catch(() => ({}));

    return { ...defaultConfig, ...userConfig };
  }

  async setConfig(pluginName: string, key: string, value: any): Promise<void> {
    const configPath = path.join(PLUGINS_DIR, pluginName, 'config.json');
    const config = await this.getConfig(pluginName);

    // Используем lodash.set для установки вложенных свойств
    _.set(config, key, value);

    await fs.writeJson(configPath, config, { spaces: 2 });
  }
}
```

## 5. Пользовательский опыт

### 5.1 Опыт установки

```bash
$ novel plugins add translate
⠋ Установка плагина translate...
✔ Плагин translate успешно установлен!

Плагин перевода успешно установлен!

Доступные команды:
- /translate: Запустить процесс перевода
- /polish: Редактирование английского текста
- /expert translate: Войти в режим эксперта по переводу

Используйте /translate, чтобы начать перевод вашей работы.
```

### 5.2 Опыт использования

В Claude Code:
```
Пользователь: /translate
AI: Обнаружена команда перевода, запускается процесс перевода...
```

### 5.3 Список плагинов

```bash
$ novel plugins list

Установленные плагины:

  novel-translate (v1.0.0)
    Плагин для перевода и локализации китайских и английских романов
    Команды: /translate, /polish
    Эксперт: Эксперт по переводу и локализации
```

## 6. Соображения безопасности

### 6.1 Песочница для плагинов
- Ограничение доступа к файловой системе
- Ограничение сетевых запросов
- Ограничение выполнения системных команд

### 6.2 Управление разрешениями
```json
{
  "permissions": {
    "fileSystem": {
      "read": ["stories/**", "chapters/**"],
      "write": ["translation/**"]
    },
    "network": ["api.deepl.com"],
    "commands": ["git", "npm"]
  }
}
```

### 6.3 Проверка плагинов
- Статический анализ кода
- Обнаружение вредоносного поведения
- Механизм сообщений от сообщества

## 7. Оптимизация производительности

### 7.1 Отложенная загрузка
- Плагины загружаются по требованию, не влияя на скорость запуска
- Плагин инициализируется только при первом использовании команды

### 7.2 Механизм кэширования
- Кэширование метаданных плагина
- Кэширование шаблонов команд
- Кэширование конфигурации

### 7.3 Параллельная обработка
- Параллельная установка нескольких плагинов
- Параллельное создание файлов команд

## 8. План тестирования

### 8.1 Модульное тестирование
- [ ] Тестирование загрузчика плагинов
- [ ] Тестирование системы внедрения команд
- [ ] Тестирование управления конфигурацией
- [ ] Тестирование совместимости версий

### 8.2 Интеграционное тестирование
- [ ] Тестирование процесса установки плагинов
- [ ] Тестирование выполнения команд
- [ ] Тестирование совместной работы нескольких плагинов
- [ ] Тестирование интеграции с AI-платформой

### 8.3 Тестирование производительности
- [ ] Производительность загрузки плагинов
- [ ] Производительность выполнения команд
- [ ] Сценарии с большим количеством плагинов

## 9. Статус реализации

### Реализованные функции ✅
- Менеджер плагинов (PluginManager)
- Система внедрения команд
- Команды управления плагинами CLI
- Интеграция с режимом эксперта
- Пример плагина перевода
- Управление конфигурацией плагинов

### Планируемые функции 📋
- Магазин плагинов
- Удаленная установка плагинов
- Управление версиями плагинов
- Больше официальных плагинов

## 10. Критерии успеха

### 10.1 Технические показатели
- Время загрузки плагина < 100 мс
- Время внедрения команды < 500 мс
- Частота сбоев плагинов < 0.1%

### 10.2 Пользовательские показатели
- Среднее количество установленных плагинов > 2 на пользователя
- Коэффициент использования плагинов > 60%
- Удовлетворенность плагинами > 4.5/5

### 10.3 Экосистемные показатели
- Количество официальных плагинов > 10
- Количество сторонних плагинов > 20
- Количество активных разработчиков > 50