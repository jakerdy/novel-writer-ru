/**
 * Инструмент автоматического преобразования методов
 * Преобразование существующего контента между различными методами письма
 */

interface StoryContent
{
  chapters: Chapter[];
  characters: Character[];
  worldSetting: WorldSetting;
  themes: string[];
  currentMethod: string;
}

interface Chapter
{
  number: number;
  title: string;
  content: string;
  structuralRole?: string; // Роль в текущей структуре
  wordCount: number;
}

interface Character
{
  name: string;
  role: string;
  arc: string;
}

interface WorldSetting
{
  time: string;
  place: string;
  rules: string[];
}

interface ConversionMap
{
  chapters: ChapterMapping[];
  structuralNotes: string[];
  recommendations: string[];
  warnings: string[];
}

interface ChapterMapping
{
  original: number[];
  target: string;
  description: string;
}

export class MethodConverter
{
  /**
   * Преобразование метода
   */
  convert(content: StoryContent, targetMethod: string): ConversionMap
  {
    const sourceMethod = content.currentMethod;

    // Выбор стратегии преобразования в зависимости от исходного и целевого метода
    const converterKey = `${sourceMethod}_to_${targetMethod}`;

    switch (converterKey)
    {
      case 'three-act_to_hero-journey':
        return this.threeActToHeroJourney(content);
      case 'three-act_to_seven-point':
        return this.threeActToSevenPoint(content);
      case 'hero-journey_to_three-act':
        return this.heroJourneyToThreeAct(content);
      case 'hero-journey_to_story-circle':
        return this.heroJourneyToStoryCircle(content);
      case 'story-circle_to_three-act':
        return this.storyCircleToThreeAct(content);
      case 'seven-point_to_three-act':
        return this.sevenPointToThreeAct(content);
      default:
        return this.genericConversion(content, sourceMethod, targetMethod);
    }
  }

  /**
   * Структура из трех актов в Путешествие героя
   */
  private threeActToHeroJourney(content: StoryContent): ConversionMap
  {
    const totalChapters = content.chapters.length;
    const act1End = Math.floor(totalChapters * 0.25);
    const act2End = Math.floor(totalChapters * 0.75);

    const mapping: ChapterMapping[] = [
      {
        original: [1, Math.floor(act1End * 0.4)],
        target: '1. Обычный мир',
        description: 'Первые 40% первого акта отображаются как обычный мир'
      },
      {
        original: [Math.floor(act1End * 0.4) + 1, Math.floor(act1End * 0.6)],
        target: '2. Зов к приключениям',
        description: 'Событие-триггер отображается как зов'
      },
      {
        original: [Math.floor(act1End * 0.6) + 1, Math.floor(act1End * 0.8)],
        target: '3. Отказ от зова',
        description: 'Период колебаний главного героя'
      },
      {
        original: [Math.floor(act1End * 0.8) + 1, act1End],
        target: '4. Встреча с наставником',
        description: 'Получение помощи или руководства'
      },
      {
        original: [act1End + 1, act1End + Math.floor((act2End - act1End) * 0.1)],
        target: '5. Преодоление порога',
        description: 'Вход во второй акт отображается как преодоление порога'
      },
      {
        original: [act1End + Math.floor((act2End - act1End) * 0.1) + 1, act1End + Math.floor((act2End - act1End) * 0.4)],
        target: '6. Испытания, союзники и враги',
        description: 'Конфликты в первой половине второго акта'
      },
      {
        original: [act1End + Math.floor((act2End - act1End) * 0.4) + 1, act1End + Math.floor((act2End - act1End) * 0.5)],
        target: '7. Приближение к самой глубокой пещере',
        description: 'Подготовка к главному испытанию'
      },
      {
        original: [act1End + Math.floor((act2End - act1End) * 0.5) + 1, act1End + Math.floor((act2End - act1End) * 0.6)],
        target: '8. Главное испытание',
        description: 'Центральный кризис отображается как главное испытание'
      },
      {
        original: [act1End + Math.floor((act2End - act1End) * 0.6) + 1, act1End + Math.floor((act2End - act1End) * 0.8)],
        target: '9. Получение награды',
        description: 'Приз, полученный после преодоления испытания'
      },
      {
        original: [act1End + Math.floor((act2End - act1End) * 0.8) + 1, act2End],
        target: '10. Обратный путь',
        description: 'Процесс возвращения во второй половине второго акта'
      },
      {
        original: [act2End + 1, act2End + Math.floor((totalChapters - act2End) * 0.6)],
        target: '11. Возрождение',
        description: 'Кульминация третьего акта отображается как возрождение'
      },
      {
        original: [act2End + Math.floor((totalChapters - act2End) * 0.6) + 1, totalChapters],
        target: '12. Возвращение с эликсиром',
        description: 'Финал и новый баланс'
      }
    ];

    const notes = [
      'Путешествие героя подчеркивает внутренний рост персонажа, требуется усилить психологическое описание',
      'Возможно, потребуется определить или добавить роль наставника',
      'Этап главного испытания должен отражать тему "смерти и возрождения"'
    ];

    const recommendations = [
      'Проверьте наличие явной роли наставника, при необходимости добавьте ее',
      'Усильте внутреннюю дугу трансформации главного героя',
      'Убедитесь, что каждый этап продвигает тему'
    ];

    const warnings = [
      'Линейное развитие структуры из трех актов может не полностью соответствовать цикличности Путешествия героя',
      'Возможно, потребуется добавить некоторые переходные главы'
    ];

    return { chapters: mapping, structuralNotes: notes, recommendations, warnings };
  }

  /**
   * Структура из трех актов в семиточечную структуру
   */
  private threeActToSevenPoint(content: StoryContent): ConversionMap
  {
    const totalChapters = content.chapters.length;

    const mapping: ChapterMapping[] = [
      {
        original: [1, 3],
        target: '1. Крючок (Hook)',
        description: 'Начальные главы как крючок'
      },
      {
        original: [Math.floor(totalChapters * 0.25) - 1, Math.floor(totalChapters * 0.25) + 1],
        target: '2. Первая поворотная точка (PP1)',
        description: 'Конец первого акта как PP1'
      },
      {
        original: [Math.floor(totalChapters * 0.375) - 1, Math.floor(totalChapters * 0.375) + 1],
        target: '3. Первая точка сжатия (Pinch1)',
        description: 'Точка давления в первой части второго акта'
      },
      {
        original: [Math.floor(totalChapters * 0.5) - 1, Math.floor(totalChapters * 0.5) + 1],
        target: '4. Середина (Midpoint)',
        description: 'Середина истории'
      },
      {
        original: [Math.floor(totalChapters * 0.625) - 1, Math.floor(totalChapters * 0.625) + 1],
        target: '5. Вторая точка сжатия (Pinch2)',
        description: 'Точка давления во второй части второго акта'
      },
      {
        original: [Math.floor(totalChapters * 0.75) - 1, Math.floor(totalChapters * 0.75) + 1],
        target: '6. Вторая поворотная точка (PP2)',
        description: 'Конец второго акта как PP2'
      },
      {
        original: [Math.floor(totalChapters * 0.95), totalChapters],
        target: '7. Развязка (Resolution)',
        description: 'Конец третьего акта как развязка'
      }
    ];

    const notes = [
      'Семиточечная структура требует четкого контроля ритма',
      'Точки сжатия (Pinch Points) должны демонстрировать силу противника',
      'Середина должна быть настоящим поворотным моментом'
    ];

    const recommendations = [
      'Убедитесь, что каждая точка имеет четкую функцию',
      'Проверьте, действительно ли середина меняет направление истории',
      'Усильте ощущение давления в точках сжатия'
    ];

    const warnings = [
      'Возможно, потребуется изменить положение некоторых глав для соответствия семиточечной структуре',
      'Ритм может потребовать корректировки'
    ];

    return { chapters: mapping, structuralNotes: notes, recommendations, warnings };
  }

  /**
   * Путешествие героя в структуру из трех актов
   */
  private heroJourneyToThreeAct(content: StoryContent): ConversionMap
  {
    const mapping: ChapterMapping[] = [
      {
        original: [1, 5], // Этапы 1-5
        target: 'Первый акт',
        description: 'От Обычного мира до Преодоления порога объединяются в первый акт'
      },
      {
        original: [6, 10], // Этапы 6-10
        target: 'Второй акт',
        description: 'От Испытаний до Обратного пути объединяются во второй акт'
      },
      {
        original: [11, 12], // Этапы 11-12
        target: 'Третий акт',
        description: 'Возрождение и Возвращение объединяются в третий акт'
      }
    ];

    const notes = [
      'Упрощение 12 этапов до 3 актов',
      'Возможно, потеря глубины деталей',
      'Требуется переорганизовать переходы'
    ];

    const recommendations = [
      'Сохраните ключевые узлы роста',
      'Убедитесь, что пропорции актов соответствуют (25%-50%-25%)',
      'Объедините схожие этапы'
    ];

    const warnings = [
      'Возможно, потеряется ритуальность Путешествия героя',
      'Дуга роста персонажа может быть упрощена'
    ];

    return { chapters: mapping, structuralNotes: notes, recommendations, warnings };
  }

  /**
   * Путешествие героя в круг историй
   */
  private heroJourneyToStoryCircle(content: StoryContent): ConversionMap
  {
    const mapping: ChapterMapping[] = [
      {
        original: [1], // Обычный мир
        target: '1. Ты (зона комфорта)',
        description: 'Обычный мир = зона комфорта'
      },
      {
        original: [2, 3], // Зов + Отказ
        target: '2. Потребность',
        description: 'Зов порождает потребность'
      },
      {
        original: [4, 5], // Наставник + Порог
        target: '3. Вход',
        description: 'Преодоление порога ведет в новый мир'
      },
      {
        original: [6, 7], // Испытания + Пещера
        target: '4. Поиск',
        description: 'Процесс испытаний - это поиск'
      },
      {
        original: [8, 9], // Главное испытание + Награда
        target: '5. Находка',
        description: 'Получение необходимого через главное испытание'
      },
      {
        original: [10], // Обратный путь
        target: '6. Цена',
        description: 'Цена, заплаченная на обратном пути'
      },
      {
        original: [11], // Возрождение
        target: '7. Возвращение',
        description: 'Возвращение после возрождения'
      },
      {
        original: [12], // Возвращение с эликсиром
        target: '8. Изменение',
        description: 'Полное изменение'
      }
    ];

    const notes = [
      'Круг историй больше подчеркивает цикличность',
      'Упрощает сложность Путешествия героя',
      'Больше подходит для коротких рассказов или серий'
    ];

    const recommendations = [
      'Подчеркните внутренние потребности персонажа',
      'Четко определите причинно-следственные связи каждого шага',
      'Рассмотрите возможность цикличности'
    ];

    const warnings = [
      'Возможно, потребуется упростить некоторые сложные сюжеты',
      'Эпичность может быть уменьшена'
    ];

    return { chapters: mapping, structuralNotes: notes, recommendations, warnings };
  }

  /**
   * Круг историй в структуру из трех актов
   */
  private storyCircleToThreeAct(content: StoryContent): ConversionMap
  {
    const mapping: ChapterMapping[] = [
      {
        original: [1, 2], // Ты + Потребность
        target: 'Первый акт',
        description: 'Установление и триггер'
      },
      {
        original: [3, 4, 5, 6], // Вход + Поиск + Находка + Цена
        target: 'Второй акт',
        description: 'Приключения и конфликты'
      },
      {
        original: [7, 8], // Возвращение + Изменение
        target: 'Третий акт',
        description: 'Разрешение и новый баланс'
      }
    ];

    const notes = [
      'Упрощение 8 шагов до 3 актов',
      'Сохранение основного конфликта',
      'Корректировка распределения ритма'
    ];

    const recommendations = [
      'Убедитесь, что второй акт не затянут',
      'Сохраните ключевые моменты трансформации',
      'Четко определите функции трех актов'
    ];

    const warnings = [
      'Возможно, потеряется особенность циклической структуры',
      'Потребуется переработать ритм'
    ];

    return { chapters: mapping, structuralNotes: notes, recommendations, warnings };
  }

  /**
   * Семиточечная структура в структуру из трех актов
   */
  private sevenPointToThreeAct(content: StoryContent): ConversionMap
  {
    const mapping: ChapterMapping[] = [
      {
        original: [1, 2], // Крючок + PP1
        target: 'Первый акт',
        description: 'От Крючка до Первой поворотной точки'
      },
      {
        original: [3, 4, 5, 6], // Pinch1 + Mid + Pinch2 + PP2
        target: 'Второй акт',
        description: 'Все промежуточные точки'
      },
      {
        original: [7], // Развязка
        target: 'Третий акт',
        description: 'Часть развязки'
      }
    ];

    const notes = [
      'Сохранение ключевых поворотных точек',
      'Упрощение точек до актов',
      'Корректировка переходов'
    ];

    const recommendations = [
      'Убедитесь, что поворотные точки остаются четкими',
      'Не теряйте чувство ритма',
      'Сохраняйте кривую напряжения'
    ];

    const warnings = [
      'Возможно, потеряется точный контроль ритма',
      'Потребуется добавить переходный контент'
    ];

    return { chapters: mapping, structuralNotes: notes, recommendations, warnings };
  }

  /**
   * Общее преобразование (когда нет конкретных правил преобразования)
   */
  private genericConversion(content: StoryContent, source: string, target: string): ConversionMap
  {
    const totalChapters = content.chapters.length;

    // Общее сопоставление на основе процентов
    const mapping: ChapterMapping[] = [
      {
        original: [1, Math.floor(totalChapters * 0.25)],
        target: 'Завязка/Экспозиция',
        description: 'Этап завязки истории'
      },
      {
        original: [Math.floor(totalChapters * 0.25) + 1, Math.floor(totalChapters * 0.75)],
        target: 'Развитие/Конфликт',
        description: 'Основной конфликт и развитие'
      },
      {
        original: [Math.floor(totalChapters * 0.75) + 1, totalChapters],
        target: 'Кульминация/Развязка',
        description: 'Кульминация и разрешение'
      }
    ];

    const notes = [
      `Преобразование из ${this.getMethodName(source)} в ${this.getMethodName(target)}`,
      'Это преобразование на основе общих правил',
      'Может потребоваться ручная корректировка'
    ];

    const recommendations = [
      'Внимательно проверьте преобразованную структуру',
      'Отрегулируйте в соответствии с особенностями целевого метода',
      'Рассмотрите необходимость добавления или удаления контента'
    ];

    const warnings = [
      'Автоматическое преобразование может быть неточным',
      'Рекомендуется ручная проверка и корректировка',
      'Некоторые особенности могут быть утеряны при преобразовании'
    ];

    return { chapters: mapping, structuralNotes: notes, recommendations, warnings };
  }

  /**
   * Генерация отчета о преобразовании
   */
  generateConversionReport(content: StoryContent, targetMethod: string): string
  {
    const conversionMap = this.convert(content, targetMethod);

    let report = `# 📝 Отчет о преобразовании метода\n\n`;
    report += `## Сводка преобразования\n`;
    report += `- **Исходный метод**：${this.getMethodName(content.currentMethod)}\n`;
    report += `- **Целевой метод**：${this.getMethodName(targetMethod)}\n`;
    report += `- **Общее количество глав**：${content.chapters.length} глав\n`;
    report += `- **Общее количество слов**：${content.chapters.reduce((sum, ch) => sum + ch.wordCount, 0)} слов\n\n`;

    report += `## 📊 Сопоставление глав\n\n`;
    report += `| Исходные главы | Целевая структура | Описание |\n`;
    report += `|----------------|-------------------|----------|\n`;
    conversionMap.chapters.forEach(mapping =>
    {
      const range = mapping.original.length === 2
        ? `${mapping.original[0]}-${mapping.original[1]}`
        : `${mapping.original[0]}`;
      report += `| Глава ${range} | ${mapping.target} | ${mapping.description} |\n`;
    });

    report += `\n## 📌 Структурные примечания\n`;
    conversionMap.structuralNotes.forEach(note =>
    {
      report += `- ${note}\n`;
    });

    report += `\n## ✅ Рекомендации\n`;
    conversionMap.recommendations.forEach(rec =>
    {
      report += `- ${rec}\n`;
    });

    if (conversionMap.warnings.length > 0)
    {
      report += `\n## ⚠️ Предупреждения\n`;
      conversionMap.warnings.forEach(warning =>
      {
        report += `- ${warning}\n`;
      });
    }

    report += `\n## 🔧 Дальнейшие шаги\n`;
    report += `1. Проверить адекватность сопоставления глав\n`;
    report += `2. Скорректировать содержание глав в соответствии с новой структурой\n`;
    report += `3. Добавить или удалить необходимый переходный контент\n`;
    report += `4. Обеспечить целостность новой структуры\n`;
    report += `5. Протестировать читательский опыт\n`;

    return report;
  }

  /**
   * Получить китайское название метода
   */
  private getMethodName(method: string): string
  {
    const names: Record<string, string> = {
      'three-act': 'Трёхактная структура',
      'hero-journey': 'Путешествие героя',
      'story-circle': 'Круг историй',
      'seven-point': 'Семиточечная структура',
      'pixar-formula': 'Формула Пиксар'
    };
    return names[method] || method;
  }
}