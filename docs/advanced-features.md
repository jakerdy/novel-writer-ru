# Руководство по использованию расширенных функций

## 📊 Система интеллектуальных рекомендаций методов

### Описание функции
Интеллектуально рекомендует наиболее подходящие методы письма на основе характеристик вашего произведения.

### Как использовать
```bash
novel recommend [options]
```

### Описание параметров
- `-g, --genre <type>` - Жанр романа (фэнтези/научная фантастика/триллер/романтика/реализм и т. д.)
- `-l, --length <number>` - Предполагаемое количество символов (по умолчанию: 100000)
- `-a, --audience <type>` - Целевая аудитория (дети/подростки/взрослые/массовая аудитория и т. д.)
- `-e, --experience <level>` - Опыт письма (начальный/средний/продвинутый)
- `-f, --focus <type>` - Акцент в творчестве (сюжет/персонажи/тема/баланс)
- `-p, --pace <speed>` - Предпочтение темпа (быстрый/средний/медленный)
- `-c, --complexity <level>` - Сложность (простая/средняя/сложная)

### Примеры использования

#### 1. Рекомендация для длинного фэнтези-романа
```bash
novel recommend --genre 奇幻 --length 200000 --complexity 复杂
```

#### 2. Рекомендация для детского рассказа
```bash
novel recommend --genre 儿童 --length 30000 --audience 儿童 --experience 初级
```

#### 3. Рекомендация для триллера
```bash
novel recommend --genre 悬疑 --focus 情节 --pace 快
```

### Результат вывода
Система предоставит:
- Анализ характеристик произведения
- Основные рекомендованные методы (с соответствием и обоснованием)
- Альтернативные рекомендованные методы
- Конкретные творческие предложения

## 🔄 Инструмент конвертации методов

### Описание функции
Помогает преобразовывать существующие проекты между различными методами письма.

### Как использовать
```bash
novel convert <target> [options]
```

### Описание параметров
- `target` - Целевой метод (обязательно)
- `-f, --from <method>` - Исходный метод (по умолчанию считывается текущая конфигурация)
- `-o, --output <file>` - Файл отчета о преобразовании
- `--dry-run` - Только генерация отчета, без фактического преобразования

### Примеры использования

#### 1. Просмотр схемы преобразования (без фактического преобразования)
```bash
novel convert hero-journey --dry-run
```

#### 2. Преобразование из трехактной структуры в семиточечную
```bash
novel convert seven-point --from three-act
```

#### 3. Создание подробного отчета о преобразовании
```bash
novel convert story-circle --output conversion-report.md
```

### Пример сопоставления преобразований

#### От трехактной структуры к семиточечной
- Начало первого акта → Крючок
- Конец первого акта → Первая поворотная точка
- Первая половина второго акта → Первая точка сжатия
- Середина второго акта → Центральная точка
- Вторая половина второго акта → Вторая точка сжатия
- Конец второго акта → Вторая поворотная точка
- Третий акт → Развязка

## 🎯 Поддержка гибридных методов

### Описание функции
Позволяет использовать различные методы письма на разных уровнях повествования для более гибкого творчества.

### Три уровня
1. **Основной метод**：Управляет общей структурой истории (основной сюжет)
2. **Вспомогательный метод**：Используется для побочных сюжетов, арок персонажей или структуры глав
3. **Микро-метод**：Используется для отдельных сцен или внутри глав

### Как использовать
```bash
novel hybrid <action> [options]
```

### Типы действий

#### 1. Просмотр информации
```bash
novel hybrid info
```

#### 2. Получение рекомендаций
```bash
novel hybrid recommend
```

#### 3. Создание гибридной структуры
```bash
novel hybrid create --primary <method> [options]
```

### Описание параметров
- `-p, --primary <method>` - Основной метод
- `-s, --secondary <method>` - Вспомогательный метод
- `-m, --micro <method>` - Микро-метод
- `--scope <type>` - Область применения вспомогательного метода:
  - `sub-plot` - Побочный сюжет
  - `character-arc` - Арка персонажа
  - `chapter` - Структура главы

### Примеры использования

#### 1. Комбинация для эпического фэнтези
```bash
novel hybrid create --primary hero-journey --secondary story-circle --scope character-arc
```
Основной сюжет — «Путешествие героя», развитие персонажа — «Круг истории».

#### 2. Комбинация для триллера
```bash
novel hybrid create --primary seven-point --secondary three-act --scope chapter
```
Общая структура — семиточечная, организация глав — трехактная.

#### 3. Комбинация для многолинейного повествования
```bash
novel hybrid create --primary three-act --secondary story-circle --scope sub-plot --micro pixar
```
Основной сюжет — трехактная структура, побочные сюжеты — «Круг истории», сцены — по формуле Pixar.

### Рекомендуемые комбинации

| Тип | Основной метод | Вспомогательный метод | Применимость |
|------|----------|----------|----------|
| Эпическое фэнтези | Путешествие героя | Круг истории (персонажи) | Длинные фэнтези, серии романов |
| Триллер | Семиточечная структура | Трехактная (главы) | Напряженный темп, коммерческие романы |
| Ансамблевое повествование | Трехактная структура | Круг истории (побочные сюжеты) | Многолинейные истории, современная литература |
| История взросления | Круг истории | Путешествие героя (ключевые главы) | Подростковые, жанр взросления |

### Выходной файл
После создания гибридной структуры будет сгенерирован файл `hybrid-structure.md`, содержащий:
- Конфигурация методов
- Сопоставление структуры
- Руководство по использованию
- Пример описания

## 💡 Лучшие практики

### 1. Начало нового проекта
1. Сначала получите рекомендации с помощью `novel recommend`
2. Рассмотрите необходимость использования гибридных методов
3. Инициализируйте проект с рекомендованными методами

### 2. Преобразование существующего проекта
1. Сначала используйте `--dry-run` для просмотра схемы преобразования
2. Сделайте резервную копию текущего проекта
3. Выполните преобразование и проверьте сопоставление

### 3. Использование гибридных методов
1. Сохраняйте последовательность в основном сюжете
2. Не позволяйте побочным сюжетам затмевать основной
3. Микро-уровень должен служить макро-уровню

## 📈 Сравнение функций

| Функция | Применимость | Требования к вводу | Результат вывода |
|------|----------|----------|----------|
| Интеллектуальные рекомендации | Выбор метода для нового проекта | Характеристики произведения | Отчет с рекомендациями |
| Конвертация методов | Изменение метода для существующего проекта | Целевой метод | Сопоставление преобразований |
| Гибридные методы | Сложная структура произведения | Комбинация нескольких методов | Документ с гибридной структурой |

## 🔗 Связанные документы
- [Руководство по методам письма](spec/knowledge/writing-methods-guide.md)
- [Подробное описание каждого метода](spec/presets/)
- [Описание конфигурации проекта](.specify/config.json)

## 📝 Журнал обновлений
- v0.4.3 - Реализованы три расширенные функции
  - Система интеллектуальных рекомендаций
  - Инструмент автоматической конвертации
  - Поддержка гибридных методов